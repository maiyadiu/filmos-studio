{
  "name": "@filmos/chatgpt-app",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "./dist/src/server.js",
  "scripts": {
    "build:contracts": "npm --prefix ../../packages/filmos-tool-contracts run build",
    "build:widget": "node scripts/build-widget.mjs",
    "build": "npm run build:contracts && npm run build:widget && tsc -p tsconfig.json",
    "check": "npm run build:contracts && npm run build:widget && tsc -p tsconfig.json --noEmit",
    "test": "npm run build:contracts && npm run build:widget && tsx --test --test-concurrency=1 test/*.test.ts",
    "test:golden-real": "npm run build:contracts && npm run build:widget && tsx --test --test-concurrency=1 test/real-film-core-golden.integration.ts",
    "start": "node dist/src/server.js",
    "doctor": "node dist/src/doctor.js",
    "grant": "node dist/src/grant-cli.js"
  },
  "dependencies": {
    "@filmos/tool-contracts": "file:../../packages/filmos-tool-contracts",
    "@modelcontextprotocol/ext-apps": "^1.7.5",
    "@modelcontextprotocol/sdk": "^1.30.0",
    "express": "^5.2.1",
    "zod": "^3.25.76"
  },
  "devDependencies": {
    "@types/express": "^5.0.6",
    "@types/node": "^22.20.1",
    "esbuild": "^0.25.10",
    "tsx": "^4.23.1",
    "typescript": "^7.0.2"
  },
  "overrides": {
    "qs": "6.16.0"
  },
  "engines": {
    "node": ">=20"
  }
}
