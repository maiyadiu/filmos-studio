---
title: 待测试
description: 影策已实现功能的手工验证清单与未确认边界
---

# 待测试

## 分镜角色字段与复制粘贴

- [x] 分镜工具明确 characters 只填来源原名；屏幕影像、画外音等说明保留在 action/camera。隔离正反例验证，虚构角色仍拒绝，不自动改名或重放旧批次。
- [x] 源码桌面宿主补标准 Edit 菜单，剪切/复制/粘贴/全选及撤销重做走焦点响应链；Swift 定向测试通过，不读取系统剪贴板或打包 App。
- [x] Web 画布不拦截普通输入、空 contenteditable、plaintext-only、浮层/弹窗及只读文字选区；隔离浏览器事件验证通过，节点复制粘贴保留。
- [ ] 当前用户源码窗口重载后的真实快捷键复测；不能把浏览器合成事件或菜单结构检查当成原生窗口完整验收。

## 项目 AI 与生成配置保存

- [x] Film Core 接收 Policy V2 多连接格式；模型锁保留在各任务路线中，不压回单连接格式。旧有 V1 验收入口保持可读写。
- [x] 前端真实 V2 构建器、HTTP 保存客户端与临时 Film Core 跨语言回归：保存、重新读取、再编辑一路而保留另一路。
- [x] 多连接预算与配置回执同事务；无效绑定、冲突或低于预留/已用额度时整笔回滚。预算回读来自真实账本，不使用页面提交的计数；后续授权精确选择对应连接账本。
- [x] 错误提示保留错误码并给出中文原因及安全字段名；配置读取失败时不允许保存覆盖。
- [ ] 用户当前源码工作台上的实际按钮复测；隔离库通过不代表已替用户保存了真实配置，不启动外部生成。

## Codex订阅从零编剧（S1）

- [x] 保留模型API入口；新建章节工具及批次回执、初稿历史、幂等/冲突/回滚和逐字回读定向通过。
- [x] Codex入口可选择已添加编剧/打磨技能；技能正文不再静默截断，数量/体积超限明确拒绝。
- [x] 单轮自动范围只涵盖本次批次及其新章的有限修订；旧章、其他项目、媒体与正式审批不自动放行。专项不替代真实完整链路。
- [x] 原生桌面UI单击→真实Codex订阅→三章初稿v1→一轮已选隔离技能→三章v2；六份历史逐字/哈希回读，初稿和其他项目保留，无模型API回退或中途确认点击。
- [x] 原生章节和修订历史复用易读组件；明暗主题、390px历史对照、当前画布章节刷新和重新进入/刷新不重发通过。窄屏画布不等于移动端Agent完整体验已验收。
- [x] 标准项目目录保存正文、逐版历史及建章回执；离线目录拒绝写入。技能目录/真实路径缺陷已修复；测试登录到期401保留已有初稿，不绕过权限、不重复创建。
- [x] 本地source已通过24项；最终提交的CI、源码prepared身份以交付回执另行核对，不以开发中指纹代替交付状态。
- [ ] 用户特定“剧本打磨”技能及真实作品质量尚未验收；隔离技能只验证完整传输、指令采用与版本链，不能冒充艺术质量认证。
- ChatGPT订阅入口仅保留计划，用户明确暂不实施，不是当前批次完成门槛。

## 本机作品目录（D1）

- [x] 默认直接系统下载目录，自选父目录；作品内部只一层分类，不额外套工作区或版本目录。
- [x] 临时库专项：幂等创建、创建前校验、跨用户/过期位置授权、目录身份变化、离线拒绝、原目录重新定位、逐字正文/历史、共享素材与本地媒体、画布改绑、内容包导出及中断恢复。
- [x] 系统选择器从源码浏览器打开，实际自选目录返回、取消保留先前位置。首轮裸后台选择器已改为访达承载，不将超时冒充成功。
- [x] 最终简化目录结构下，默认/自选均通过真实浏览器新建、导入两章、修改一处、保存、刷新和导出。两项目ID、各章版本、30个受管文件及ZIP内部摘要独立核对一致；明暗/390px窄屏和打开原文件夹通过。
- [x] Agent原有提示词保存合同接上同一目录，原请求重放不新增版本、提示词正文与项目目录一致；本增量未重发真实模型任务，不把服务合同回归声称为新的自然语言验收。
- [ ] 完整SOURCE_VALIDATION、当前提交远端CI与源码准备身份核对。
- [ ] 用户真实作品日常使用；旧作品迁移、跨机器一键恢复、自动生成换图未执行。

## 本机开发免登录与启动恢复

- [x] 复用源码宿主既有desktop_local管理员模式；明确本机绑定和服务端开关，公开account登录/注册不删除，不提升其他账号或迁移数据。
- [x] 会话请求/初始化失败不再清空为游客；恢复前阻止工作区挂载，提供有界请求超时与手动重试。后端数据库故障/禁用账号不冒充匿名200。
- [x] 隔离HTTP：无Cookie稳定管理员、公开匿名/管理员拒绝、非本机绑定拒绝免登录及数据库/禁用账号错误保真；51项Web会话/用户隔离测试、Go定向及前端类型检查通过。
- [x] 浏览器：源码UI本机免登录；旧login地址直接进入工作台且0公开登录媒体请求；公开登录/注册保留；会话断线和账号初始化失败均不跳登录且重试恢复，明暗/390px窄屏可读。全部为临时库，无用户数据测试。
- [x] 游客不请求账号模型目录、不启动账号同步；不存在游客配置时清除前一账号内存配置但保留其持久化数据。公开登录回归发现并修正原先吞401的隐含依赖，失败记录保留。
- [ ] 用户真实项目的日常源码启动体验；不以fixture代替真实数据验收，不打包App。

## 已有镜头图片诊断（M5，进行中）

- [x] 指定业务镜头的只读资源/正文/来源组合检查及真实 MCP image block 接线；同名不误选、错项目/读取中变化/缺图/坏图/超限/旧哈希拒绝；浏览器与 Agent 的定向测试通过，不修改原图。
- [x] 图片编码不重复进入工作台事件或恢复历史；图片身份和源约束保留。实际解码、哈希、尺寸、单帧和有效期由 MCP 图片出口复核。
- [x] 实际 Codex 订阅自然语言视觉对照通过：四张同名合成图按真实像素/哈希逐一判断，另一个缺图明确拒绝；五项有界分类符合冻结答案。观察事实、引用约束、判断/未知/建议分开，不外推复杂真实作品准确率。
- [x] 原生工具结果可打开哈希锁定原图与当时完整易读来源；深色宽屏、浅色窄屏、滚动及历史重开通过。资源响应被替换时真实窗口拒绝展示，不借用当前其他图；不标作当前来源或QC批准。
- [x] 同一真实 Codex 会话在重新取得相同像素后，只在两行图片稿末尾追加修正重点，分别保存v1；当前/原请求/旧版v0由模型及独立接口、数据库核对通过，原稿、业务镜头、其他行和四张原图保留。首次因隔离登录过期401零写入停止，恢复登录后原会话续接，不冒充零异常验收。
- [x] 修正后的结果卡可打开原生提示词历史查看器，完整原文和追加段落易读，历史查看不会触发再次保存。视觉增量本地完整SOURCE_VALIDATION 24/24及回执/日志哈希通过；远端CI和源码产物身份由交付回执单独核对。
- [ ] 用户真实作品质量、复杂场景准确性和实际生成换图未验收，不以合成对照或文字修订替代。

## 完整工作台创作验收（M6，进行中）

- [x] 在临时数据库上进入实际Router/AppProviders、原生章节/画布/Agent面板；原生登录、功能权限及当前章节绑定已回读。没有App打包、生产数据或生成任务。
- [x] 原生章节侧边预览复用既有HTML/Markdown转换和阅读组件，恢复场景/动作/对白段落，采用14px正文与语义前景色；实际五段全文逐段一致、浅深色可读、无横向溢出。
- [x] 既有阅读器接入已锁定的中文强调解析依赖，处理中文标点紧邻粗体标记；真实章节页中的说话人已正确加粗且无裸星号，原始Markdown和修订号不变。SSR/段落/流式/表格/代码及HTML安全定向测试、Web类型和冻结锁文件核验通过。
- [x] Agent 面板内保留紧凑反馈入口；面板显示时全局反馈浮层不再遮挡发送按钮。实际打开/取消反馈弹窗未写入问题；创作画布不弹出遮挡操作的模型入门引导，普通工作区及原设置入口保留。正式页面实测发送按钮能接受正常指针点击。
- [x] 新画布首次发送的会话创建失败时解除发送/等待状态、保留草稿且可再次发送；合成429仅验证原生UI恢复，创建请求1次、模型turn请求0次，没有真实额度消费或API回退，不冒充真实账号额度失败验收。
- [x] 原生分镜首次挂载不重复保存默认编辑区高度；修复后完整原生正常组、增量组及恢复组均通过，真实手改和并发CAS仍拒绝。部分保存/后置核验失败显示“结果待核对”，保留公开409及回读提示，不宣称零写入。
- [x] 三组隔离自然语言任务已由原生Router/Agent完成并独立核对数据库、接口、旧历史和同名对照：正常全链、局部修订、丢回复与取消续接。恢复组实际发生确认超时，十五分钟授权到期后另续一个只读轮次完成最终复核；不是零异常或无需续接的样例。
- [x] 确认超时实际返回的 `AGENT_CONFIRMATION_EXPIRED` 已补入公开409分类；定向回归通过，守卫时限不变。三组保存证据绑定窄修前源码，后续分类修正由对应回归及源码CI独立覆盖，不覆写旧回执。
- [x] 本批本地24项SOURCE_VALIDATION通过，包含创作持久化、原生UI/账号隔离、会话/Broker、Agent构建及源码Supervisor单测；不含App打包或真实模型生成。远端提交/CI和源码运行身份另由交付回执记录。
- [x] 隔离视觉组合已验证真实像素→诊断→对应两行文字修订→保存/历史回读；仅合成样例，非自动图片替换或用户真实作品验收。

## Agent 连续会话接线（M4，未整体交付）

- [x] 原生计划事件、消息稳定身份、完整正文和历史回放接线；换行、重复分片、最终全文校正、外会话/外轮次隔离、中断计划保留及同会话记录重开通过定向测试。
- [x] 移除连接状态变化时无条件清空聊天的逻辑；当前作用域变更与普通重连分开处理。
- [x] 真实 Codex 只读会话的实时 Markdown 表格、分段正文及浅/深色主题和760px窄屏；空闲后实际服务断线与恢复保留历史、表格及未发送草稿。文字进度列表不再误生成重复快捷按钮。
- [ ] 当前真实会话未提供原生 update_plan，结构化计划卡尚未实证；不可用时诚实显示文字进度，不伪造计划事件。实际文字步骤与逐步业务回读已通过，结构化卡片不作为新的阻塞门禁。
- [x] 运行状态只读观察接线：从现有live turn和确认映射回读；断线不判完成，未核对状态前禁发新任务，历史读取不resume/旋转授权/重发保存。定向执行中读取、取消、状态过期与读路径测试通过；真实忙碌重开体验待验。
- [x] 隔离真实Codex会话执行中重开后停止入口恢复、新建/历史切换禁用；停止精确命中原轮次，运行时和UI随后均非忙碌，原provider/业务数据保留。取消后同provider实际重新回读完整正文、两镜和两稿，版本/依赖/旧历史精确一致。另一次重开上下文503被模型如实报告并停止后续步骤；完整产品三组验收另归M6。
- [x] Agent 工具结果复用剧本修订历史、章节分镜与提示词编辑器；隔离浏览器验证了指定剧本 v2 对照、提示词历史 v1 与当前 v2 分开、原 provider 历史恢复后的结果入口，以及浅/深色易读展示。错误、待确认、缺失行或跨项目结果不生成保存入口；业务内容与历史回读未变。
- [x] 本机安全会话到期清空上下文后返回明确的 `canvas_context_unavailable`（503）和回读提示；安全会话十分钟、上下文五分钟时限不变。定向撤销/重连与公开错误测试通过，原瞬断的完整时序与真实运行中恢复仍待验。
- [x] Canonical 确认摘要追加授权项目/章节或画布行、基版和修改数量，不包含正文或提示词；保留原风险说明，定向测试通过。真实确认卡在新运行时中的展示仍待验。
- [x] 实际确认卡与拒绝路径已验证：本地冲突不再冒充后端409；单次真实Go409经过桥接返回安全分类与回读提示，拒绝请求零落库。standalone工具确认不虚构正在运行的模型轮次，ready状态确认前后稳定。
- [x] 复合创作、业务镜头导入原画布、失败依赖停止、增量修改、结果定位和完整产品路由的三组文本验收通过；计划和聊天自报均未代替数据库/版本回读。

## 当前画布分镜同步（M4 接入中）

- [x] `project_sync_storyboard` 与原生已有画布导入共用单画布保存路径，固定业务项目/当前画布，保留节点和行身份、提示词、素材及连接。
- [x] 服务端内容版本守卫、缺失基线拒写、响应丢失精确回读、用户切换、手工冲突后自动保存不得反向覆盖、无关本地修改保留、关联失败续接及来源失效的隔离测试通过。
- [x] 隔离真实 Codex 复合任务与仅改首镜的增量任务已回读核验；原文、第二镜和旧提示词历史保留。首轮中途修复画布时间基线后恢复同一 provider 会话，不声称零人工干预的完整最终验收。
- [x] M6 三组完整文本任务、失败/忙碌恢复与原生页面结果导航通过；整体源码交付与视觉验收仍分别待办。

## 分镜提示词持久化接入（M3，未整体交付）

后端已在原画布字段上接入版本、同库追加历史/回执、来源依赖、请求重放和并发保护。原生分镜表与Agent工具共用提示词保存入口；隔离组件验证了保存/关闭重开/旧版回读、响应丢失恢复、并发拒绝且保留草稿，以及明暗主题和窄屏长文。真实Codex会话已为隔离目标镜头编写两类草稿各一版并回读；长正文触发GET参数过长的问题已修复，回读只允许四项定位参数，模型通过原请求恢复未重复写。提示词保存不代上传未同步本地素材。还须完成来源变化后的真实局部修订及完整产品路由验收。界面复用已有易读渲染，不以HTML标签/Markdown标记作为阅读正文，原文独立保存；模型、实图未绑定则不声称已适配或视觉验证。详见[数据库合同](/docs/backend/backend-database#日常分镜提示词接入中)。

## Agent 脚本修订

- [x] 原文/修订追加历史、旧章节迁移、重开 SQLite 后回读、幂等重发、CAS 冲突、跨用户/跨项目拒绝和事务失败回滚的隔离测试通过。
- [x] 前端修订工具经过共享 API 客户端、真实 HTTP、Go Handler/Service 和临时 SQLite：只修改目标对白，原文保留，修订回读哈希一致，重复请求不新增版本，陈旧请求返回 409。
- [x] 浏览器中的真实修订历史组件已检查前后文本、差异范围、初始版本选择；不是截图模拟界面。
- [x] 隔离源码浏览器中，真实 Codex 订阅会话经正式 Agent 面板、MCP、共享 API、Go 与 SQLite 完成短章/36段长对白的指定修订、原文保留及逐字回读。不是工具参数直调或假模型；尚不等于完整用户工作台全流程验收。
- [x] 待确认写入时“停止本轮”不新增修订，实际 provider 会话恢复保持同一 thread；新普通创作会话未使用 shell/文件修改。响应丢失幂等重试、手工并发修改优先与 Broker 取消边界通过定向测试。
- [x] 隔离页面与真实修订历史组件复用章节易读模式，检查 HTML/Markdown、前后版与文字差异、浅/深色主题、36段正文和760px窄窗口。测试壳与正式页面的整合验收分开记录。
- [ ] 完整项目/画布入口的连续脚本→业务分镜→提示词端到端验收、取消恢复与结果定位；后续实施不以本段局部验收代替。
- [ ] 用户真实章节的创作质量、长文本与实际使用习惯确认；本轮不使用真实作品作自动化测试。
- [ ] 已有 Film Core 正式锁定与本次编辑历史的后续联动，另行按业务范围实现；本轮不会自动批准/锁定。

## Agent 业务分镜

- [x] 同库结构化镜头、脚本来源、版本、追加历史与幂等批次实现；隔离测试验证全批回滚、并发竞争、稳定ID、原素材引用保留、旧表迁移及源脚本变化标记过期。
- [x] 原生Agent工具和共享API已同步批量合同；读工具按只读风险登记，保存为非付费写。项目绑定、回执不匹配和历史回执不是当前结果的工具测试通过。
- [x] 真实HTTP工具链复测通过：响应丢失重试不重复、局部修改保留无关镜头、手工修改阻止陈旧覆盖、来源变化标记过期；修复ORM时间戳与回执不一致后，精确落库回读一致。
- [x] 保存前原文引用覆盖、对白逐字/说话人/顺序校验，连续跨镜拆句、局部范围保留、旧镜头过期过滤测试通过；不将引用保真标为语义导演质量。
- [x] 长任务上下文过期的定向修复：显式读取工作台后绑定精确新凭据，旧写请求/审批/跨项目仍拒绝；MCP回传脱敏错误码和续接提示。16项上下文/权限/MCP测试通过。
- [x] 真实零模型调用探针复现已加载Codex会话保留旧MCP环境；每BrainSession隔离进程，授权轮换仅替换对应进程后原thread读取新环境成功。27项进程/适配/取消/权限测试与构建通过；真实创作续接仍单独验收。
- [x] 真实Codex自然语言完成37段/25对白的15镜拆镜，再只改第10镜camera；其余14镜、原剧本与旧版本精确不变。文本动作/道具/站位已审查，不冒充图片质量验收。前几轮上下文/授权配置故障已修复，失败记录保留。
- [x] 正式章节组件的易读分镜/历史、原生画布编辑器的15行内容与后端逐字段匹配；真实画布保存、重开和重复导入保持单一节点及关联。明暗主题和760px阅读窗口通过；移除误带整章容器大内边距。
- [x] UI拒绝来源过期导入和手工映射覆盖，拒绝前后后端数据完全一致；单测覆盖额外手工行、提示词/素材引用保留。项目概要补回shotRevision，定向HTTP与同库服务重启后回读一致。
- [ ] 部分前端业务错误经Agent桥接后的安全分类保真、最终任务结果定位，在连续任务体验中继续处理；当前不宣称所有错误恢复均完善。
- [ ] 完整工作台入口与源端到端回归；不将基础保存测试标为整个分镜能力完成。

## 源码基线整改

- 普通 push/PR 切换到单独标识的 `SOURCE_VALIDATION`；App 全量验收保留为显式授权入口。远端行为须在获准推送后由实际新 CI 验证，不把本地修改写成已部署。
- 真实宿主/生命周期测试现在需要显式真实数据测试授权；普通验证只运行临时数据与定向 Supervisor 测试。源码 smoke、单测、隔离 HTTP 与用户真实运行验收分开记载。
- Review Bus Node 要求及缺少 SQLite authorizer 的失败关闭已补齐；不得由此自动启动历史封存或 external-read Runtime。
- 后续 App 专属界面/安装验收仍等待用户允许成熟打包；下列历史 App 验证记录不构成当前自动打包授权。

## RC1 前 Agent 生命周期

- [x] Browser OpenAI/DeepSeek/Local 会话按各自 Profile 完成 create/cancel/close，跨 Profile 操作失败关闭。
- [x] 七种 Brain Profile 独立探测全部返回结果，单项异常不会拒绝整个矩阵，且探测不触发 Provider/API/生成请求。
- [x] ChatGPT Host 已通过两个独立进程的持久化恢复 Gate：保留 BrainSession、Host Session、Project/Domain Project/Canvas，并轮换新 Agent Grant；跨项目恢复被拒绝。
- [x] ChatGPT Handoff 使用 `waiting_host`、可哈希 Receipt 和 `host.handoff.prepared` / `host.observed` / `host.proposal.received` / `host.handoff.expired` 事件，不伪造 ChatGPT 消息或“本轮完成”。
- [x] 恢复历史按真实边界展示：Codex 来自 Provider `thread/read`，ChatGPT 来自 Handoff 时间线，API/Local 显示未持久化限制。
- [ ] 在安装后的桌面 App 中手工确认 Handoff 卡片的“打开 ChatGPT”和“查看连接状态”两个按钮、明暗主题和窄窗口布局。

## FilmOS ChatGPT 连接管家

- [x] Runtime Key 仅通过 macOS Keychain 保存，不进入参数、偏好设置、日志、诊断或 Acceptance Artifact。
- [x] Film Core、FilmOS MCP、Project Grant 与官方 Tunnel Client 由同一 `ServiceSupervisor` 管理，支持启动、有界重连和 App 退出清理。
- [x] Project Grant 续期并轮换 MCP 后，连接监控会等待最后一次 Tunnel 重启的真实就绪状态，再自动重放同项目工作台上下文；确定性 Swift 回归测试已覆盖延迟就绪边界。
- [x] External Activation 交接生成器分别绑定 `canvas_state_hash` 与当前 `filmos-live:*` Receipt，并要求 Live Context 顶层、binding 与 Blockers 三处 Receipt 精确一致；旧 Grant/Challenge 的交接失败关闭。
- [x] `issue_get_evidence` 的公开 Schema 固定声明可选 `expected_project_id`；错误项目同时返回文本 `PROJECT_SCOPE_DENIED` 与结构化错误码，SDK 参数格式错误仍走 `INVALID_ARGUMENT`。
- [x] MCP 继续暴露 20 个只读工具、0 个写工具；`NO-OPENAI-MODEL-API-001` 禁止所有 OpenAI 模型推理 API 路径。
- [x] Tunnel 连通与 ChatGPT 真正读到 FilmOS 分轴记录；只有带短期 Transport Proof 的真实 MCP 读取才能记录 Challenge、Request ID、Tool、Project Scope 和 Result Hash。
- [ ] 在已关联 Tunnel 权限的 OpenAI 账号中完成首次 Runtime Key 录入，确认界面到达 `WAITING_FOR_CHATGPT`。
- [ ] 由外部 ChatGPT 经 Secure MCP Tunnel 完成 Acceptance Project 的真实只读 Live Gate；在收到外部回执前不标记 `CHATGPT_REACHED_FILMOS`。

## FilmOS macOS 本地数据与备份

- [x] 主工作台与 ChatGPT 连接窗口关闭后保持可重用，程序坞再次打开不再访问已释放窗口；Swift 回归测试覆盖 `isReleasedWhenClosed = false` 合同。
- [x] 本地更新统一安装到 `~/Applications/FilmOS Studio.app`，替换前将旧 App 备份到 FilmOS Application Support，并在 `--relaunch` 模式停止其他 FilmOS 桌面实例，避免程序坞同时出现两个运行图标。
- [x] 在本机安装包上完成“关闭主窗口→程序坞重开→同一 PID 存活→无新增 Crash Report”的运行演练。
- [x] 自包含 App 不依赖源码目录、Downloads 访问权限或 Bun，内置后端和 Web 服务只监听 `127.0.0.1`。
- [x] 本地数据固定保存到 `~/Library/Application Support/FilmOS Studio/WorkbenchData`，并提供“打开 FilmOS 数据目录”菜单。
- [x] `.filmosbackup` 使用 SQLite 一致性快照，保留资源及逐文件 SHA-256 manifest，排除凭据密钥、Cookie、CLI 登录凭据和 WebKit 缓存。
- [x] `DESKTOP-BACKUP-RESTORE-001` 已在两个干净临时数据目录间完成 HTTP 导出、manifest/hash 校验、恢复、后端重启和 Golden 项目读回。
- [ ] 用户在已安装 App 中手工确认两个菜单、保存面板、覆盖提示和 Finder 定位。
- [ ] 正式恢复 UI 尚未开放；当前恢复演练使用验收工具，恢复后 Provider 密钥与 CLI 登录凭据需重新录入。

## FilmOS Golden C 与 Production Canvas

- [x] Film Core 空间版本、精准 STALE、重启、SQLite 备份恢复、事务故障和幂等重放已通过本地自动化。
- [x] Production Canvas 默认关闭、Human 确认、revision/hash 守卫、双 Service 并发唯一性与审计回滚已通过本地自动化。
- [ ] 用真实生产备份演练 SQLite → PostgreSQL 迁移；本阶段未获授权触碰用户正式数据。
- [x] Playwright 真实浏览器已验证项目详情页、Human 二次确认、创建回执、刷新复用唯一关联、画布导航和关闭开关后的 Host 连续性。
- [ ] 真实 Dreamina/Comfy/Flova 或其他 Provider 生成仍需单独授权；Golden C 只使用本地回执并保持 Candidate。

## RunningHub 与 ComfyUI Bridge 工作流

- [x] 后端全量测试、前端类型检查、Canvas Agent 构建和原生 Bridge Go 测试通过。
- [x] 覆盖后端重启后请求领取、结果先落库再恢复、原请求 ID 幂等复用，以及 RunningHub API Key 不进入 URL 的自动化测试。
- [ ] 使用真实 RunningHub Workflow 和 App 分别验证参数发现、图片/视频/音频输入映射、提交、轮询、取消和结果下载。
- [ ] 使用真实 ComfyUI API 工作流验证 Bridge 注册、心跳、领取、完成、取消和大文件结果回传。
- [ ] 在 Bridge 执行中重启后端，确认结果回传成功且任务不重复执行；在请求未领取时重启后端，确认原请求继续投递。
- [ ] 验证浅色/深色主题、窄屏设置页、画布工作流节点和任务中心状态展示。

## AI 提示词优化器

当前代码和生产构建已完成，但以下场景仍需要在真实配置和不同窗口尺寸下由用户确认：

- [x] `web` 前端 TypeScript 检查与生产构建通过。
- [x] 优化面板打开、关闭不再等待 Popover 入场或退出动效。
- [ ] 在 `/create` 中分别测试图片和视频模式的打开、发送、取消、失败重试和关闭。
- [ ] 在图片、视频画布节点中测试优化入口、结果采用和原提示词回填。
- [ ] 逐项测试五种优化方式，确认模式切换不会丢失当前输入。
- [ ] 测试优化模型切换，并确认目标生成模型与优化模型显示和请求职责分离。
- [ ] 测试已连接图片、文本参考，以及“结合参考”没有参考内容时的提示和禁用状态。
- [ ] 测试长提示词、长优化结果、滚动、拖动和四边缩放；确认窗口接近边缘时不会跑出视口。
- [ ] 测试浅色/深色主题和窄窗口布局，确认按钮、输入框和结果文本仍可读且可操作。
- [ ] 使用自定义文本渠道实际发送一次，确认渠道、模型和权限配置正确时不会误报“必须指定系统渠道和模型”。

用户确认上述场景后，再将对应条目移动到功能文档的已验证范围。

## 肖像可识别性排查画布插件

首版画布节点、本机签名运行时、本地 ONNX 预检、百度候选链路、模型作业协调器和四种报告导出已接入；以下边界仍需要真实环境确认：

- [x] Agent TypeScript 构建、Web TypeScript 检查通过。
- [x] 本机任务持久化、幂等、取消/重试清理、候选 URL 安全校验和报告转义测试通过。
- [x] Markdown、无脚本 HTML、DOCX 共用检测结论、本地预检、六维特征、修改建议和失败候选说明；报告结构回归测试通过。
- [ ] 使用固定真人/虚拟人图片建立 Python 原实现与 TS 实现的检测框、embedding 和风险金标。
- [ ] 在安装 `det_10g.onnx`、`w600k_r50.onnx` 的 Windows 环境完成直接比对和本地仅模式验证。
- [ ] 在系统 Chrome/Edge 可用环境完成百度识图、验证码、页面结构变化和候选下载失败验证。
- [ ] 配置支持双图输入的项目视觉模型，验证 Path A、Path B、模型租约、并发、取消和页面重开恢复。
- [ ] 浏览器确认插件首次关闭、旧节点只读、明暗主题、窄屏、键盘焦点和候选添加到画布。

用户确认上述场景后，再将对应条目移动到功能文档的已验证范围。

## 画布节点自动整理

自动整理入口和布局提交逻辑已完成，以下场景仍需要在实际画布中手工确认：

- [x] `web` 前端 TypeScript 检查与生产构建通过。
- [x] 画布底部视图控制中显示“自动整理节点”按钮。
- [ ] 无选中节点时，确认多个顶层节点会按连线流程布局；无连线时会按网格布局。
- [ ] 选中节点时，确认只移动选中的可移动节点，未选中节点保持原位。
- [ ] 确认锁定节点、容器节点和折叠批次隐藏子节点不会被移动。
- [ ] 点击“撤销”确认可以恢复整理前的位置，并确认刷新画布后布局仍能保存。
- [ ] 在浅色/深色主题、窄窗口和节点数量较多的画布中确认底部按钮可见、可操作且不遮挡主要内容。

## 分镜脚本资产关联

1. 在同一画布放入角色、场景图片、参考视频和音频后生成分镜，确认各镜头只关联模型返回的真实画布资产，不出现失效或编造 ID。
2. 分镜卡片按“序号、时长、视频提示词、台词/旁白、关联资产”五列显示；图片、视频和音频缩略图可点击并打开对应预览。
3. 从单个镜头及批量入口创建视频节点，确认脚本行输出和每个关联资产都自动连到视频节点，重复同步不会产生重复连线。
4. 打开视频节点提示词，确认关联资产以标准 `@资产` 缩略图展示，生成请求能解析图片、视频和音频引用。
5. 修改镜头资产关联后再次同步，确认已移除的托管资产连线被清理，而用户手工创建的普通连线仍保留。

## 零价免费模型

1. 管理员设置启用系统渠道模型的按次价格为 `0` 后，普通用户刷新设置即可看到并成功创建任务。
2. 前台逻辑模型使用统一按次价格 `0` 后，普通用户可见并能成功创建任务。
3. 零价任务完成后，账单状态为已结算，用户可用积分和预留积分均不变化。
4. 零价 Token 任务在上游未返回 usage 时仍能以 `0` 结算；正价 Token 任务继续依赖 usage 结算。
5. 管理端提交任一负价格仍会被拒绝。
6. 未勾选价格已配置的默认零价模型仍不会向普通用户公开。

## 用户诊断包导出

当前实现已完成前后端链路、脱敏和设置页 UI，但仍需要真实环境验证下载链路和不同错误场景：

- [x] `web` 前端 TypeScript 检查与生产构建通过。
- [x] 诊断服务已覆盖脱敏、ZIP 结构和跨用户任务拒绝测试。
- [x] 设置页桌面布局和 680px 窄屏布局已完成浏览器预览。
- [ ] 登录后从设置页导出成功，确认 ZIP、诊断编号和文件名均可用。
- [ ] 从失败任务详情进入诊断页，确认 `taskId`、`projectId` 和对应日志范围正确带入。
- [ ] 后端未重启或接口未部署时，确认前端能显示可操作的错误提示，而不是原始 Blob/502 文案。
- [ ] 验证浅色/深色主题、15 分钟到 24 小时范围、长问题描述和导出中的 loading 状态。

## Eagle 素材库

- [x] 增加素材无文件夹、无标签时的空集合归一化单元测试。
- [ ] 使用真实 Eagle 服务验证无文件夹素材、无标签素材、文件夹路径和预览引用。

## Windows 本地开发

- [ ] 在一台依赖已安装和一台全新工作区分别运行 `.\scripts\start-local.ps1`。
- [ ] 验证端口被占用时脚本会安全退出，且不会关闭已有进程或使用正式开发数据目录。

## 画布生成结果资源恢复

- [x] `web` 前端 TypeScript 检查与生产构建通过。
- [ ] 模拟图片生成任务已成功但结果下载失败，确认节点显示“重新加载资源”和“重新生成”两个按钮。
- [ ] 点击“重新加载资源”，确认只重新查询并消费原任务结果，不创建新的生成任务或重复扣费。
- [ ] 模拟视频结果落盘/OSS 上传失败后再次加载，确认恢复成功会清除错误状态并保留原任务信息。
- [ ] 点击“重新生成”，确认仍走原有生成重试流程；普通生成失败仍只显示“重新生成”。

## 数据同步与资源删除重构

- [x] 前端远端基线、登录 hydrated 屏障、上传并发编辑和删除持久化回归测试通过。
- [x] Generation 失败只回滚生成字段，保留同节点并发标题编辑；重复 Generation 仍按明确 effect key 幂等。
- [x] 资源引用识别改为显式字段合同，未登记的字段名和自由文本不会被推断为资源引用。
- [x] 后端素材删除改为数据库事务 + `resource_deletion_jobs` Outbox，数据库失败时物理文件保持不动。
- [x] Outbox Worker 本地文件删除、对象不存在幂等成功和失败退避测试通过。
- [ ] 使用真实 OSS 配置验证对象删除成功、权限失败后的退避重试和多实例租约接管。
- [ ] 浏览器真实环境验证登录快照慢响应期间工作区保持不可写，以及删除后立即刷新不复活素材。

## ChatGPT Host 与即梦 CLI 本机连接

- [x] ChatGPT Host 状态改为校验当前项目、Tunnel、Project Grant、只读 MCP 合同和状态时效，不再用 Local Runtime 在线状态冒充“ChatGPT 已连接”。
- [x] 未就绪的 ChatGPT Host 会阻止 Agent 提交并给出“连接设置”入口；已保存连接会在首个 Film Project 激活后自动恢复。
- [x] macOS App 会从受控位置发现官方即梦 CLI；正式打包入口精确装载 Canvas Agent 与 Dreamina 模块，并要求健康检查明确返回 `dreamina_module_loaded`。
- [x] App 原位更新会终止旧主进程及安装目录内的孤儿 Helper，避免旧 Runtime 继续占用端口导致新包看似未生效。
- [x] Web 正式构建、Agent/Dreamina 专项测试与 Desktop 48 项 Swift 验收测试通过。
- [ ] 用户从程序坞打开重新安装的 App，确认“生成引擎”显示即梦模块已加载并能读取当前账号登录状态。
- [ ] 在已授权 Film Project 中重新打开 ChatGPT 连接，确认 Agent 先显示“Host 已就绪，等待 ChatGPT 接管”，外部 ChatGPT 完成真实读取后才显示“ChatGPT 已连接”。

## V2.4 最终生产权威

- [x] Acceptance 与普通项目共用 `ProductionGenerationService`；本地 Mock 仅作为 Provider Adapter，Provider 网络请求与费用均为 0。
- [x] Composer 与 Agent 共用 Canonical Broker；Film Core 拒绝合成 Receipt，并只创建正式 GenerationPackage、GenerationAttemptEvidence 与 Candidate。
- [x] Budget Reserve、Submit、Settle、Release、Reconcile 进入真实持久账本，重启后可按事件复核 Projection。
- [x] Dreamina、Flova、RunningHub、ComfyUI 状态统一进入 Engine Connection 同步器；Project Policy、Model Lock 与 Budget 在项目设置中持久化。
- [x] ChatGPT 当前项目上下文改为完整 SHA-256，并保留精确的 Host context invalid 错误。
- [ ] 用户从最终固定 Commit 重装的 App 验证项目级生成设置、Engine 状态与 ChatGPT Host 交互文案。

## Review Bus Installed SourceIdentity

- [x] 新 Intake 不再从固定历史 Commit 推断源码，必须同时验证 App Bundle `SourceIdentity.json`、`InternalRuntime.json`、Developer Repository Locator 和实际 Git Commit/Tree。
- [x] 缺失或漂移时按 `INSTALLED_SOURCE_IDENTITY_UNAVAILABLE`、`INSTALLED_SOURCE_IDENTITY_MISMATCH`、`SOURCE_COMMIT_NOT_FOUND`、`SOURCE_TREE_MISMATCH` 或 `APP_RUNTIME_IDENTITY_MISMATCH` 失败关闭。
- [x] Architecture v2 的初始 Intake Evidence 可持久化，但不伪造 Requirement Delta；Owner 冻结 Delta 后由 Coordinator 确定性进入正式 Evidence Frozen。
- [x] Review Bus 51 项测试、Coordinator 12 项专项测试、Desktop 61 项 Swift 测试及 Developer Repository 脚本测试通过。
- [x] Stage B 本地 Canary 完成 Installed SourceIdentity、Owner Gate、v2 Evidence、Assessment 起点及历史 Anchor 链验证，外部请求与付费操作均为 0。
- [ ] 待 Stage B 固定 Commit 后执行 Formal CI、Artifact 校验、Installed App 可回滚替换与实机 Canary。

## 治理稳定 V1.1｜Host Boundary 与仓库清理

- [x] Web 与 macOS Desktop 已统一使用有界 RPC 生命周期，保留原 action 名、超时、尺寸限制和安全错误码。
- [x] 五个 Host 贡献槽及 Review Contract 由单一合同生成，并通过导入边界测试阻止 Film Domain 反向依赖宿主私有实现。
- [x] 清理前打包 Canary 已证明 Review Bus 单一 Submission、Receipt、Projection 和事件链；真实用户数据计数及元数据哈希前后一致。
- [x] 历史运行日志、Playwright 截图、提交式 Widget bundle 和重复 pnpm 锁文件已从当前树移出；308 个原对象均由 `governance/清理恢复清单.json` 绑定来源 Commit、SHA-256 和恢复命令。
- [x] Widget runtime 改为构建时生成到忽略目录；ChatGPT 36 项、Review Bus 51 项、Chrome Bridge 8 项、Web Host Boundary 7 项及 Swift build 已通过。
- [ ] 待独立清理 Commit 后在干净 clone 重放固定工具链、同一 Vertical Canary、Formal CI 与 Artifact 校验。
- [ ] 全部验收通过后执行 Installed App 可回滚替换，并再次确认真实项目数据计数与元数据哈希不变。
