# 治理稳定 V1.1 检查点

## 当前目标与状态

- 持续目标：依次完成 Stage A、Stage B、Stage C1、Stage C2，不在单一阶段提前结束。
- 当前阶段：Stage A Control Plane Recovery。
- 工作区：`/Users/apple/Downloads/other/短剧/wt-governance-stage-a-v11`
- 分支：`codex/governance-stage-a-v11`
- Stage A 已安装基线提交：`7d61df95fc847e3883089816c61faeb89ea62e09`
- Stage A 已安装基线树：`d2f581ca35f609dd0b9a6228d1db725fffa60c8e`
- Installed App 当前身份：`candidate-7d61df95-d2f581ca`，已与固定源码同步并通过全部 Helper 健康检查。

## 锁定边界

- Review Bus 是 Lane 和正式 Issue ID 的唯一权威。
- Stage A 只处理 Submission、附件暂存、Finalize、Receipt、读回和当前 `b327...` 草稿迁移。
- 当前 Submission：`FILMOS-SUBMISSION-b3274782-30a0-44a1-a05e-01730678da8b`。
- 目标 Issue：`FILMOS-ARCH-b3274782-30a0-44a1-a05e-01730678da8b`。
- 不修改 main、不创建 RC1 Tag、不调用模型 API、付费 Provider、素材上传或外部项目创建。
- 回执持久化后只允许 GET/read 重试，禁止再次 POST、Finalize 或附件重传。

## 已完成

- 冻结共识合同与阶段边界。
- 新建独立 Stage A worktree；暂停 worktree 保持不变。
- Review Bus Submission、不可变附件、原子 Finalize、一次性 Bootstrap、Receipt、MCP Read Receipt、SQLite 一致性备份已实现。
- Desktop WebKit 桥接已切换为 Submission、Attachment、Finalize 三步合同。
- Web 已拆分 `local_draft_id`、`submission_id`、`canonical_issue_id`，实现 Receipt 后只读恢复与迁移 Tombstone。
- ChatGPT MCP 实际读适配会写入 `x-filmos-read-consumer: chatgpt-mcp`。
- Stage A 业务代码及历史入口收口已固定为 `831153e65c1e27697422e9c9a0ed347467dc8115` / `c22b8464e7598c453506d85076b779382e827347`。
- 正式 CI Run `33544895047` 已通过；Artifact `9815418169` 的嵌套 `EVIDENCE_INDEX.json`、Release Manifest 与远端 GitHub API 已由 `GitHubEvidenceVerifier` 独立核验，11 项检查均为真。
- Installed App 替换前已使用 SQLite 一致性备份保存 Workbench 与 Review Bus，并保存 WebKit、工作台配置和 Evidence 目录；备份位于 `~/Library/Application Support/FilmOS Studio/UserDataBackups/StageA-20260901T185338Z`。
- 首次本地候选虽通过旧的静态 bundle 验证，但启动时 Film Core 因 PyInstaller 选择了缺少 Uvicorn 的 Python 而失败；错误候选已隔离到 `AppBackups/Rejected-StageA-20260902-025650.app`，Installed App 已回滚且主进程与既有 Helpers 可用。
- 已增加打包前 Python 完整依赖门禁，以及打包后真实启动 Film Core 并读取 `/health` 的运行门禁；在这两个门禁、更新后的正式 CI 和新一致性数据备份全部通过前，不再替换 Installed App。
- 打包门禁修复已固定为 `7d61df95...`；正式 CI Run `33547647960` 和 Artifact `9816354324` 通过，GitHubEvidenceVerifier 11 项全真。
- 使用完整 Python 从干净源构建并安装 Installed App；Film Core、Review Bus、Local Runtime、Backend、Web 与 Canvas Agent MCP 均已真实运行。
- 替换前一致性备份位于 `~/Library/Application Support/FilmOS Studio/UserDataBackups/StageA-20260901T191728Z`；替换前后 Workbench 项目数、Review Bus Projection/附件数和 Film Core 正式记录数保持。
- External Intake 中确认旧 `b327...` 草稿及原文仍在固定 `43100` WebKit origin；点查迁移未产生 v2 草稿且静默失败。源码已修正为枚举识别历史记录，无活动项目时保持等待且不消耗重试，项目切换时 Fail Closed；待新固定 CI 与 Installed App 验证。

## 验证证据

- Review Bus：40/40 通过。
- Web 专项：9/9 通过；TypeScript 通过。
- ChatGPT MCP 专项：2/2 通过；TypeScript check 通过。
- macOS 产品与专项验收通过；本机 Swift 测试共 61 项、11 个 Suite 全部通过。
- 首次正式 CI 中固定 Swift 6.0.3 Desktop Release Build 通过。
- Python 依赖负向门禁：不完整的 `/usr/local/bin/python3` 被正确拒绝；完整验收 Python 可导入 PyInstaller 6.22.2、FastAPI 0.141.1、Pydantic 2.13.4、Uvicorn 0.52.4。
- shell 语法、Python 编译、静态合同、桌面专项验收、打包后 Film Core 健康检查与 `git diff --check` 均通过。
- 新增历史草稿保真迁移与项目作用域门禁两项合同测试；Web 专项当前 11/11、TypeScript 通过。

## 当前后续

1. 完成旧草稿枚举迁移、无项目等待与项目事件立即重放的最小修复，跑全部 Stage A 回归。
2. 固定新 Commit/Tree，重跑正式 CI/Artifact，再执行一次一致性备份和 Installed App 同步。
3. 在正确项目上下文中完成 `b327...` External Intake、Receipt、Projection/Evidence 及 ChatGPT MCP 读回。
4. 完成 Stage A 独立验收后进入 Stage B Architecture Protocol，而不是结束持续目标。
