# FilmOS Agent Contracts

FilmOS 原生多脑运行时的共享 TypeScript 合同。该包只定义 Brain、Connection、Session、Context、Tool、Confirmation 和 Hosted Handoff 协议；ChatGPT 公共 MCP 工具合同继续由 `@filmos/tool-contracts` 维护。

```bash
npm run build
npm test
```
