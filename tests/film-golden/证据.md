# 第二阶段 Web / Golden A 证据

## 已验证入口

- `VITE_FILM_PRODUCTION_CORE=true` 与 `VITE_FILM_PRODUCTION_CANVAS=true` 必须同时显式成立；任一关闭时不请求 Sidecar、不产生 Film DOM。
- 只接受带显式端口的本机 HTTP Sidecar，`GET /health` 必须返回 `status=ok` 与 `service=film-production-core`；端口不可用时入口不出现。
- Project 页面复用现有 ContentUnit、CanvasUnitLink 与 Production Canvas 导航决策。唯一 `production` 关联可打开；重复关联显示全部 Canvas ID 并 fail closed；缺失关联只显示创建预演，不调用旧 storyboard 创建路径。
- 关闭、端口不可用、恢复、再次关闭均有组件级静态 DOM 测试；既有章节和 storyboard 文件未修改。

## 已验证本地域段

`golden_a_local.ts` 直接调用真实 `projectProductionCanvas`、`compilePromptDraft`、
`prepareSubmissionPackage` 与 `importManualProviderResult`：

- `prepared=true`，但 `persisted/reviewed/approved=false`；
- GenerationPackage 为 `prepared/not_submitted`；
- ManualImport 结果为 `candidate/pending/not_approved`；
- stale Shot version 与 DirectorUnit hash 漂移均 fail closed；
- 外部 Provider 调用数为 0。

## 运行结果

- Web 入口、Production Canvas、Prompt、Provider、本地 Golden 段：34 pass / 0 fail，120 assertions。
- 真实 Sidecar operation preflight：2 pass / 0 fail；临时端口与临时 SQLite，未启动持久服务。
- 旧离线 Golden A 回归：4 pass / 0 fail；仍只算离线 Mock。
- `bun run typecheck`：通过。
- `bun run build`：通过；只有既有 Vite 大 chunk 警告。

## 当前阻断

本批运行时 Core 尚未合入 D-0005 七个正式 HTTP operation，因此真实 runner 回执为
`BLOCKED_MISSING_CORE_OPERATION`，并明确记录
`prepared/persisted/reviewed/approved=false`、`external_provider_calls=0`、
`fallback_mock_used=false`。完整 HTTP Golden A 不得据此标记为 PASSED。
