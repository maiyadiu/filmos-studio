# 第二阶段验收矩阵

> 状态分轴：`NOT_RUN` / `PASSED` / `FAILED` / `BLOCKED`。此表的测试状态不代表业务对象已获批准。

| 验收面 | 当前状态 | 通过条件 | 证据位置 |
| --- | --- | --- | --- |
| 共享 Schema/OpenAPI | PASSED | Prompt 血缘、Provider 回执、Director/Coverage/Continuity 无重复 DTO；实现路由标记准确 | Schema `0.2.0`；17 paths（16 implemented / 1 planned） |
| Film Core 持久化 | PASSED | Golden A 对象使用 Core UUIDv4；写入有 version/hash 守卫；审计追加 | `film-core/tests/`：37 pass / 0 fail |
| Candidate 人工门 | PASSED | QC 前不得批准；Agent 不得批准；Human Approval 绑定当前 hash | Core 合同测试与真实 Golden 回执 |
| MCP 默认关闭 | PASSED | 默认/画布模式新工具 0；显式开启只新增 5 个 Film 工具 | `canvas-agent/test/`；工具集同步门通过 |
| MCP 真实 Sidecar | PASSED | 真 HTTP Read → Preview → Apply；version/hash/canvas 任一漂移拒绝 | 真实临时 SQLite + loopback HTTP 门禁通过 |
| Web 默认关闭 | PASSED | Flag off 时 DOM 和 Host 旧流程不变；端口失败隔离 | Film Web 78 pass；浏览器关闭态 DOM=false |
| Production Canvas | PASSED | 只投影 Film ID；重复 production canvas 阻断；不伪造正式创建 | Web 单测与真实 Host 项目浏览器预演 |
| Manual Provider | PASSED | 本地 fixture/人工导入；拒绝密钥、URL、绝对路径/data URL；外部调用 0 | Provider 测试与真实 Golden 回执 |
| 真实 Golden A | PASSED | 真 Core HTTP + 真 Prompt compiler + 真 Manual import + Human Approval 全链，无 Mock Store | `tests/film-golden/`：2 pass，`fallback_mock_used=false` |
| 浏览器入口/CORS | PASSED | 本机 Sidecar 健康检查可跨域；开启显示，关闭不进入 DOM | `implementation/test-reports/浏览器GoldenA.md` |
| 冲突/恢复/回滚 | PASSED（本阶段范围） | 过期写入不产生事实；中途失败可重放；关 Flag 恢复 Host | stale guard、当前 hash 重试、浏览器 Flag 回滚均通过 |
| 外部生成/上传/远程发布 | PASSED（范围未授权） | 本阶段不执行，调用数必须为 0 | Golden 回执：`external_provider_calls=0` |

## 当前总门禁

- Core：37 passed，1 条 Starlette TestClient 依赖弃用警告。
- Contracts：Schema `0.2.0`，17 paths（16 implemented / 1 planned），6 状态轴；OpenAPI 零漂移。
- Canvas Agent：338 passed / 0 failed / 5 个 Windows 专项 skipped；Film MCP 已接入共享入口但默认关闭。
- Web 原生：471 passed；Film 专项 78 passed / 228 assertions；TypeScript 与生产构建通过，保留既有大 chunk 警告。
- Golden A：真实 HTTP 2 passed；离线 Mock 回归 4 passed；外部调用 0。
- Backend：`go test ./...` 通过；迁移沙箱 8 passed；Desktop 14 passed。

## 状态边界

- `prepared`、`persisted`、`reviewed`、`approved` 是独立证据，不互相代替。
- 浏览器验收覆盖 Host 登录/项目/章节、Film 入口、本机 CORS、预演和 Flag 回滚；正式对象全链由真实 HTTP Golden 证明，不宣称已在浏览器逐步操作全链。
- 唯一 production canvas 正式创建 API、Impact 查询和完整灾难恢复仍不在本阶段通过范围。
