# 第二阶段验收矩阵

> 状态分轴：`NOT_RUN` / `PASSED` / `FAILED` / `BLOCKED`。此表的测试状态不代表业务对象已获批准。

| 验收面 | 当前状态 | 通过条件 | 证据位置 |
| --- | --- | --- | --- |
| 共享 Schema/OpenAPI | NOT_RUN | Prompt 血缘、Provider 回执、Director/Coverage/Continuity 无重复 DTO；实现路由标记准确 | `film-contracts/`、`tests/film-contract/` |
| Film Core 持久化 | NOT_RUN | Golden A 对象使用 Core UUIDv4；写入有 version/hash 守卫；审计追加 | `film-core/tests/` |
| Candidate 人工门 | NOT_RUN | QC 前不得批准；Agent 不得批准；Human Approval 绑定当前 hash | Core 合同测试与 Golden 回执 |
| MCP 默认关闭 | NOT_RUN | 默认/画布模式新工具 0；显式开启只新增 5 个 Film 工具 | `canvas-agent/test/` |
| MCP 真实 Sidecar | NOT_RUN | 真 HTTP Read → Preview → Apply；version/hash/canvas 任一漂移拒绝 | `canvas-agent/test/` |
| Web 默认关闭 | NOT_RUN | Flag off 时 DOM 和 Host 旧流程不变；端口失败隔离 | `web/test/` |
| Production Canvas | NOT_RUN | 只投影 Film ID；重复 production canvas 阻断；不伪造正式创建 | `web/test/` |
| Manual Provider | NOT_RUN | 本地 fixture/人工导入；拒绝密钥、URL、绝对路径/data URL；外部调用 0 | Provider 测试与 Golden 回执 |
| 真实 Golden A | NOT_RUN | 真 Core HTTP + 真 Prompt compiler + 真 Manual import + Human Approval 全链，无 Mock Store | `tests/film-golden/` |
| 冲突/恢复/回滚 | NOT_RUN | 过期写入不产生事实；中途失败可重放；关 Flag 恢复 Host | Golden/Recovery 回执 |
| 外部生成/上传/远程发布 | NOT_AUTHORIZED | 本阶段不执行，调用数必须为 0 | Golden 回执 |

## 基线

- Core：10 passed，1 条依赖弃用警告。
- Contracts：Schema `0.1.0`，12 paths（8 implemented / 4 planned），6 状态轴。
- Film Agent 离线网关：13 passed；实际 MCP 未注册。
- Prompt + Production Canvas 领域测试：15 passed。
- Golden A：离线 Mock 4 passed；真实纵向链 `NOT_RUN`。
