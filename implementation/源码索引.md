# 影策 Release 源码索引

基线：`v1.2.1` / `61b332583c4fcbf71890ae67e3f0f104d67706b9`。本文只是 Program Integrator 的交叉核查，不替代各 Track 在编码前阅读入口、调用方、配置、锁文件和相邻测试。

## 桌面与 Workspace

- 当前 Release 没有 `desktop/` 目录，Swift 源文件数为 0。
- `docs/design/workspace-shell-design.mdx` 是 Web 工作区外壳设计，不是 macOS AppKit 壳。
- Track 01 不得把计划中的“现有 Swift/AppKit 壳”当作已验证事实；应核查历史/参考后按 `BUILD` 或 `DEFER` 裁决。

## Provider 与本地 Runtime

- Dreamina 实现丰富：`canvas-agent/src/dreamina-*`、`canvas-agent/src/modules/dreamina-*`、`web/src/services/local-dreamina-*` 及相邻测试。
- Comfy Bridge 跨 `backend`、`canvas-agent/native/comfy-bridge`、`web/src` 存在。
- 生产路径中未找到 Flova；Track 10 应先检查 CLI 真实安装/能力，再决定 Adapter，不得伪造已集成状态。

## 3D 导演台

- 当前 Release 有 `web/src/components/canvas/director/`、`web/src/lib/canvas/director/`、`web/src/types/director.ts` 和状态 Store。
- 包含 Camera、时间轴、关键帧、骨骼姿态、3D 视口和 Prompt 编译线索。
- 未找到以 director/3D 命名的专项测试文件；不排除间接覆盖，Track 09/13 必须继续核查。

## Remote / Hybrid

- `web/src/services/user-data-sync.ts` 有画布/资产远端同步 helper。
- 当前没有 `LOCAL_AUTHORITY` / `REMOTE_AUTHORITY` / `HYBRID_LOCAL_AUTHORITY` 影视权威模式。
- Track 12 必须将“现有用户数据同步”与“Film 正式事实权威/选择性发布”分开。
