# Codex Thread Handoff

Current objective: 完成 V1.1 双专家真实运行闭环最终纠偏。

Active workspace: `<LOCAL_WORKTREE>/wt-v1-1-dual-expert-operational-closure-v1`

What is already done: 固定基线 worktree 已创建；任务包完整读取；WP-00 至 WP-06 的本地实现已完成。Review Bus 已支持 Candidate A→B、原子 Findings、Consensus、Issue Task Package、完整 Evidence、一次性 Chrome 配对、Review Center、GitHub Run/Artifact 远程验证与 Live Trace；用户反馈弹窗可粘贴截图，章节可切换易读/Markdown。Codex Coordinator 已打包进 Local Runtime，并改为固定基线的独立可写 Git worktree；普通画布 Agent 仍保持只读/按请求授权。桌面安装器会在 Application Support 内建立无网络本地源码镜像，避免访问 Downloads 触发 TCC。原生风险字段桥接已恢复，Review Bus 现提供仅限本机主令牌的全项目待办发现，Codex Coordinator 无活动画布时也会以反馈自带项目 ID 和隔离 review canvas scope 自动接管。

Locked decisions / must preserve: V1.1 Constitution；单一 Review Bus/Film Core/Agent Runtime/Candidate/Tool Broker；ChatGPT MCP 只读；写回必须用户手势；OpenAI 模型 API 与真实 Provider 费用均为 0；不修改 main；不创建 RC1 Tag。

Important files and paths: `services/filmos-review-bus`、`canvas-agent/src/brains/review-codex-coordinator.ts`、`canvas-agent/src/brains/review-worktree-manager.ts`、`cli/filmos-review`、`services/filmos-chatgpt-app`、`extensions/filmos-review-bridge`、`desktop/macos/scripts/prepare-review-source-repository`、`web/src/components/governance`、`web/src/pages/admin/review-center`、`acceptance`。

Completed verification: Review Bus 25/25；Review CLI 3/3；Chrome Bridge 6/6；反馈 UI；Codex Coordinator/Worktree 专项回归；Canvas Agent production build；Web production build；Evidence Index、V1.1 静态 Gate、进程级自动化 Candidate A→B Gate 均 PASS。完整 `rc-local` 已在单一 receipt `20260831T142332Z-ecfc79a9b9f7-rc-local` 中全绿通过。GitHub Attempt 1 `33403136054` 真实暴露 Node 22 SQLite ExperimentalWarning 与 JSON artifact 同流时的解析缺陷；修复候选 `5039f944ea96b22ce2ad9fbbe1586f0eefee2111` 的 Attempt 2 `33404083234` 已在全新 macOS Runner 全量通过。自动风险路由候选 `4e9798fa5db6adbdd9b8356c31d0c4e17af1c600` 的 GitHub Run `33407138710` 已全量通过；原生桥接修复候选 `d91360fadeca65fd54033ae1078415b3e7e080f7` 的 GitHub Run `33409340391` 已全量通过，并按硬规则替换稳定 App。真实项目 `b3bb06d2f52c9eeff4da08bba05b6d02` 的反馈 `FILMOS-ISSUE-79e36199-aae2-49fc-9c09-6495d808f75c` 已由 App 重放进入 Review Bus `core / EVIDENCE_FROZEN`，事件链与完整证据包已冻结；随后现场又复现“无活动画布时协调器不发现项目反馈”的缺口，现已增加本机内部全项目发现和无画布回退回归测试。使用 Acceptance 专用 Python 的 Canvas Agent 完整串行套件单次通过：`407 pass / 0 fail / 5 platform skip`，包含真实 Film Core HTTP 两项和 Dreamina P0 竞态；Review Bus 仍为 `25/25`，V1.1 自动化预检与 Evidence Index 均 PASS。无画布协调候选 `94e5a7c088f66b74d60509ea2877d8a17e881e0d` 的 GitHub Run `33416050532` 已在全新 macOS Runner 全量通过。UI 使用纠偏候选 `8052a0f43f469eed3895d493a516340695f83a00` 的 GitHub Run `33418790936` 已全量通过，并按硬规则替换稳定 App；App 现场确认整本导入、2 章预览与反馈截图入口。后续真实文件 `月老_剧本_001-020_V3.2.md` 又暴露 `第001集｜标题` 竖线未识别和 macOS WKWebView 缺少文件面板回调；修复候选 `427071f2961d42969ebc48af3c671052535cdc40` 的 GitHub Run `33422757766` 已全量通过，稳定 App 现场经原生文件面板只读选择该真实文件，明确得到 `20 章`、首尾 `001/020` 与按钮 `导入全部 20 章`，随后取消且没有写入项目。继续真实 Roundtrip 时又定位到第二层 P0：Review Coordinator 虽能发现无活动画布的 Issue，但通用 Runtime 仍强制抓取活动画布快照，Codex adapter 也强制执行 `workbench_get_context` 预检，导致真实协调首轮以“当前没有已连接画布”失败。当前补丁为 `review_coordinator` 使用冻结 Issue/Evidence 的确定性隔离上下文并跳过活动画布预检，普通 Agent 保持原有严格实时上下文；专项 `12 pass / 0 fail`、Canvas Agent production build 均通过。完整 Agent 回归除本机未配置 Film Core Python 依赖的两项 HTTP 集成外为 `408 pass / 0 product fail / 5 platform skip`；这两项属于本机测试环境依赖缺失，不是本补丁回归，GitHub Acceptance 将在正式依赖环境复核。上述为本地/自动化证据，不冒充外部 Live Gate。

Latest field evidence: 无活动画布补丁 `e9024da2e12fec57d030f5e3d1b5e65592ef673d` 的 GitHub Run `33425132884` 已完整通过，稳定 App 也已按硬规则替换。真实重启继续暴露旧会话恢复 P0：本地保存的 coordinator provider thread 已无对应 Codex rollout，真实 `thread/resume` 明确返回 `no rollout found for thread id`；旧实现会在写入 `RUNNING/FAILED` 前反复恢复同一失效会话。当前修复仅为 `review_coordinator` 在该精确错误下创建替代 provider thread，普通 Codex 会话仍 fail-closed；所有 session/prepare/ensure 阶段错误会写入可观察的 `codex_coordination=FAILED`。专项 `20 pass / 0 fail` 与 production build 已通过。

External Live Gate field evidence: rollout 恢复候选 `72ccb09ef548b8bf308f1016499ee5c572dee8e8` 的 GitHub Run `33427127469` 已完整通过并替换稳定 App。真实 Review Coordinator 已恢复失效 provider thread，并对目标 `FILMOS-ISSUE-79e36199-aae2-49fc-9c09-6495d808f75c` 完成独立 Codex assessment 写回，Issue 进入 `CHATGPT_ASSESSING`。Chrome `FilmOS Review Bridge 1.1.0` 已由用户加载并使用 6 位一次性码完成配对；现场又发现固定 `bottom: 20px` 会覆盖 ChatGPT 输入区发送按钮，当前将写回按钮上移到输入框上方并增加位置回归断言，主 Roundtrip 保持等待首次 ChatGPT 用户手势写回。

Latest runtime recovery evidence: 首次 ChatGPT assessment 真实写回后 Issue 进入 `CONSENSUS_PROPOSED`，同一持久 Codex coordinator 会话随后暴露 `AGENT_GRANT_NOT_FOUND`。根因是 Grant 仅存于当前 Runtime 内存，但 `hydratedSessions` 曾把会话误判为可继续；新实现在每次 turn 及 coordinator ensure 前校验 Grant 签名、scope 与有效期，仅对 `AGENT_GRANT_NOT_FOUND/EXPIRED` 轮换新 Grant，signature/scope 异常仍 fail-closed，且用 session 级单飞锁防止并发双恢复。新增真实 Runtime composition 回归已覆盖“已水合会话丢 Grant”与“新进程恢复持久会话”；专项 `4 pass / 0 fail`、Chrome Bridge `6/6`、Canvas Agent production build 通过。完整 Agent 回归为 `412 pass / 0 product fail / 5 platform skip`；另有 2 项 Film Core HTTP 因本机未配置专用 Python 测试依赖而未运行，与本补丁无关。

Chrome launch crash evidence: 用户提供的 incident `D53B1388-99C4-48FE-8061-179C3AF2947A` 是一个存活 71 ms 的第二 Chrome 直接启动进程，在 HIServices `_RegisterApplication` 阶段 `SIGABRT`；已存在的用户 Chrome 主进程没有崩溃。产品的两个真实外链入口是 WKWebView 新窗口与 ChatGPT/Tunnel 设置按钮，旧实现均直接调用 `NSWorkspace.shared.open`。现已统一收口到 `ExternalURLRouter`：同一时刻只发出一个系统 URL 打开请求、重复 URL 去重，并显式设置 `createsNewApplicationInstance=false` 以复用已存在的默认浏览器实例。新增串行/去重/非 Web URL 回归，原生产品已编译通过。

Current external consensus evidence: 稳定 App 与 Tunnel 下的 ChatGPT 插件已通过“刷新”重新发现 16 个 Review Bus 只读工具。在已存在 Chrome 中完成真实 `issue_list_pending -> issue_get_evidence -> issue_get_constitution -> issue_get_codex_assessment_blind -> review_get_consensus_proposal -> review_get_decision_template` 调用；ChatGPT 精确核对项目 `b3bb06d2f52c9eeff4da08bba05b6d02`、Evidence Manifest `f896bf08d166898206b2d3831d291365c4e436e86ae2d088826331334126a7a9`、Constitution `a61228c66e931cb977928f4d2864ab6556f3fcd163479e31ccebbc6fccf39d41` 和 Proposal `f277e7040ffc079fe82c7e65faa9e96ab72d71d4754943f06db50bd320371dfd`，并经 Chrome 用户手势成功写回 `CHANGES_REQUESTED`。当前双方均为 `CHANGES_REQUESTED`，Issue 保持 `CONSENSUS_REVIEW`，Candidate/Task Package 仍为空。现场同时证明 Bridge 只扫描 `<pre>`，无法识别 ChatGPT 直接输出的精确 JSON；已增加最新回复文本的平衡 JSON 提取，仍经严格 Decision Envelope 校验。Review Bus 现增加追加式 `assessment_round_history` 与 `next-consensus-round`，仅当双方均已响应且至少一方要求修改时归档旧 Assessment/Proposal/Responses，新轮 Assessment 自动绑定 Project/Evidence/Constitution；专项 Review Bus `26/26`、Bridge `7/7` 已通过。

Open issues: 完成本轮全量回归、Commit/Push/GitHub CI；按硬规则重打包并替换稳定 App；重载 Review Bridge 并验证无代码块的原始 JSON 直接回传；启动追加式第 2 轮 Assessment，双方接受新 Proposal 后冻结 Task Package；继续 Candidate A→B 真实 Roundtrip、双签、machine PASS、UI Golden 与唯一逐字节一致 Handoff ZIP。

Next exact steps: 跑全量验证后提交推送本轮 Consensus/Bridge 修复；重打包替换 App；在不启动新 Chrome 的前提下代理用户完成第 2 轮 Assessment/Consensus 写回；再完成 Candidate A→B、双签、机器 PASS 与唯一 Handoff ZIP。

Round 2 live evidence: Codex 订阅评估已由常驻 Coordinator 写入 Round 2；ChatGPT 在既有 Chrome 中严格只调用 `issue_list_pending`、`issue_get_evidence`、`issue_get_constitution` 完成盲审。现场写回首先暴露旧页面选区优先于最新 Assistant 回复，误将上一轮 `CHATGPT_CONSENSUS_DECISION` 送出并被 Review Bus 以 `CONSENSUS_PROPOSAL_REQUIRED` 正确拒绝；当前 Bridge 已改为优先选择最新 `data-message-author-role=assistant` 回复，仅接受位于该回复内的用户选区，并把按钮上移至 `bottom: 180px`。扩展重载及页面刷新后，Round 2 `CHATGPT_ASSESSMENT` 已成功写回，双方评估绑定 Round 2、项目 `b3bb06d2f52c9eeff4da08bba05b6d02`、Evidence Manifest `f896bf08d166898206b2d3831d291365c4e436e86ae2d088826331334126a7a9` 与 Constitution `a61228c66e931cb977928f4d2864ab6556f3fcd163479e31ccebbc6fccf39d41`，新 Proposal 为 `f84251cd6bd9c041f2e9b4efc2ae2faff5951c6f4af53ee05001feac8fa12bd1`。同时修复全局 pending 队列串行耗尽导致高优先级 Proposal 被多项历史 Evidence Capture 饥饿：每个 tick 只执行一个订阅 turn，并优先 Consensus、Verdict 和 Candidate 状态，下一 tick 必须重新读取队列。

Keychain rebuild evidence: 硬规则重打包后的本地 Swift 可执行文件默认使用随源码变化的 ad-hoc `cdhash` 作为 designated requirement，导致旧 Runtime Key 的 Keychain ACL 再次触发 SecurityAgent；同步读取发生在 App 主 actor，现场采样确认会把工作台阻塞在 `SecItemCopyMatching`。当前将自动读取改为 `LAContext.interactionNotAllowed=true`，签名变化时只 fail-closed 而不弹出或冻结；本地构建器同时给主可执行文件写入稳定的 `designated => identifier "com.filmos.studio.localbeta"`，验收脚本精确校验该合同。现有 Keychain 项目可能需要一次性重新提交 Runtime Key；之后同一稳定 identifier 的重打包版本可复用授权，密钥仍只保存在 macOS Keychain。

Round 4 coordinator recovery: Round 3 双方均 `CHANGES_REQUESTED` 后追加创建 Round 4，Issue 正确回到 `EVIDENCE_FROZEN`，但订阅协调器长期跳过该新轮次。根因是 pending summary 的 `coordination_key` 只包含 state、evidence、assessments、consensus、task package、candidate、findings 与 verdicts；连续共识轮次在同一冻结证据下会产生完全相同的 `EVIDENCE_FROZEN` 指纹，进程内 `handled` 集合因此把 Round 4 误判为已处理。当前将 `assessment_round` 与 `current_round` 纳入协调指纹，并增加连续 Round 2→3、同状态同 Evidence 仍必须产生不同 key 的回归断言；Review Bus `26/26` 通过。

Final root closure implementation: `FILMOS-ISSUE-final-root-closure-20260901` 在隔离分支 `codex/review-filmos-issue-final-root-closure-20260901` 上从冻结基线 `0954599e0590bf82e60a5762e0eb8794a0ad58db` 实施。GitHub Artifact 验证改为只接受外层 Artifact 根部唯一的 V1.1 Handoff ZIP，并只读取其中与容器同名目录下唯一的 `EVIDENCE_INDEX.json`；缺失、重复、非法嵌套与声明字节哈希漂移均 fail closed。Live Trace 按 Issue 事件、追加式 Assessment Round 投影哈希和完整 Candidate Binding 核对所有 ChatGPT 写回，保留恰好两次 Candidate Review。Task Package 冻结后的 `text/plain` 崩溃报告改走独立 `evidence.supplemented` 事件，限制为有效 UTF-8 且不超过 1 MiB，只外投影哈希、大小、媒体类型、时间与 redacted alias；原 Evidence Manifest、Task Package、Candidate History、Verdict Binding 与 Dual Signoff 均保持逐对象不变。Scoped build 通过，Review Bus `31/31` 通过；正式 Commit、Push 与 GitHub Acceptance 证据以本次 Review Coordinator 的 Candidate 回执为准。

Do not do: 不使用模型 API；不执行外部付费/上传/项目创建；不改 main；不建 RC1 Tag；不以静态字符串检查冒充 Live Gate。

Final live closure evidence (supersedes earlier open-state notes): Candidate A `43c11fe01dbd5462d73fe42ae8dc2c86f87c269a`（GitHub Run `33452354478`）已由 ChatGPT 给出两项正式 Findings 后标记 `SUPERSEDED`；Candidate B `42fd448659d146ac687a55e419bd02a85fbc6f45`（GitHub Run `33459048542`、Artifact `9782632936`）关闭两项 Findings，并由 ChatGPT `EXTERNAL_APPROVED`、Codex `LOCAL_ACCEPTED`、Machine `PASS` 完成双签，Open P0 为 `0`。真实追加式历史保留 6 次 ChatGPT Assessment、6 次 Consensus 和 2 次 Candidate Review；Live Trace 不再把合法多轮历史误判为重复写回。用户最新 Chrome 崩溃报告已作为冻结 Task Package 之后的追加证据写入，attachment SHA-256 为 `0a0aae548cb1fd64c3606d4a0cd14e2185a7cedb214aafeda31a22fac5149f1a`，原 Evidence Manifest、Task Package、Candidate 与双签状态保持有效。`实时闭环证据.json` 状态为 `PASSED`，事件链完整、真实外网请求为 `0`、OpenAI 模型 API 为 `0`、真实 Provider 操作和费用为 `0`。

Chrome crash resolution: incident `D53B1388-99C-48FE-8061-179C3AF2947A` 的第二 Chrome 注册冲突已由 `ExternalURLRouter` 的串行、去重与 `createsNewApplicationInstance=false` 收口。最终 App 验证必须使用包含该修复的根提交重打包并替换用户级 canonical `FilmOS Studio.app`，禁止使用 `open -n` 启动 Chrome。

Remaining exact steps: 完成根分支全量回归、Commit/Push/GitHub CI；使用最终 CI Artifact 内 Handoff 作为唯一用户 ZIP并逐字节核对；按硬规则重打包、替换并验证稳定 App。

Final root closure V3 (supersedes the remaining-state line above): 用户现场的“连接应用未成功”不是再次点击即可恢复。稳定路径 App 仍是旧 `pilot-12457455-122ccf49`，且 ChatGPT 插件把 Secure Tunnel 注入短期 Project Grant 的只读 MCP 误配置为 OAuth。Issue `FILMOS-ISSUE-final-root-closure-v3-20260901` 已完成盲评与双专家 Consensus，Task Package `adb4506742436af80666d950deb9e9eee50bdca708af9e5c4889b1e8a0264022` 已冻结。Candidate A `344f8edd7dd0060dab449a956e740d6a20fcebe5` / GitHub Run `33463399263` / Artifact `9784107897` 通过机器验收，但 ChatGPT 以三个固定 Findings 正确拒绝：noauth 合同、Review Bus 多轮证据闭环、已安装 App 身份。

Candidate B intent: 本文件所在最终 Commit 只在冻结的 12 个文件范围内完成以下收口：所有 ChatGPT-facing 只读工具发布 noauth 兼容元数据；OAuth well-known 路径返回确定性 `OAUTH_NOT_CONFIGURED` JSON 404；Secure Tunnel 继续作为 Project Grant 注入边界；Review Bus 支持唯一实际嵌套 Evidence Index 并在缺失或重复时 fail closed；合法多轮 Assessment/Consensus 不再被 Live Trace 误判为重放；Task Package 冻结后的崩溃报告只追加、不改写 Evidence/Candidate/双签。最终运行验收必须在该 Commit 的正式 CI 之后构建 canonical `FilmOS Studio.app`、原子替换稳定路径，并核对 SourceIdentity、稳定 designated requirement、单实例和服务健康；ChatGPT 开发者设置必须为“无身份验证”，不得再进入 OAuth。

V3 hard boundaries: `OpenAI model API calls = 0`、`paid provider operations = 0`、`external uploads/projects = 0`；不修改 main，不创建 RC1 Tag。最终 Handoff 与用户 ZIP 必须来自同一个正式 GitHub Artifact 且逐字节一致。

Current UI/connection closure addendum: 根分支 `ee5af7bdf2a30876667f82ad69c7ee17078d9406` 上正在补齐真实打包 App 的动态 UI Golden，覆盖 18 个任务包界面，并修复反馈弹窗粘贴截图时尝试改写只读 `File.lastModifiedDate` 导致 React 卸载白屏的问题；Web typecheck、反馈专项 8/8、真实打包 App 18/18 动态取证均已通过。用户最新“连接应用未成功”现场核验显示：已安装稳定 App 仍是 `candidate-ee5af7bd-7ee8a7d0`，FilmOS 主程序、Runtime、Film Core、Review Bus、36 个只读 MCP 工具和 Secure Tunnel 当前均在线，Tunnel 已成功获取元数据并初始化 MCP；失败发生在 ChatGPT 连接授权阶段，且 Tunnel 仍记录 noauth MCP 下的非致命 OAuth discovery 警告。并行 Review Coordinator 候选 `d2afe88e56253160fcae629748366d1206a06ed2` 正在 GitHub Run `33470032021` 验证 Review Bus 多轮生命周期，但它从共同祖先分叉，尚未包含根分支 noauth/App 收口和本轮 UI 修复，必须在最终根提交中整合后重新验收。

Current crash finding: 用户粘贴的崩溃报告进程是 Google Chrome 151 的短命启动进程，责任进程为 FilmOSStudioDesktop，异常发生在 HIServices `_RegisterApplication`；不是 FilmOS 主进程的同类崩溃。现有 `ExternalURLRouter` 已完成串行、去重并设置 `createsNewApplicationInstance=false`；本轮由正式 Swift CI 与最终 App 验证该既有收口，不再追加缺少新复现证据的启动路径重写。

Immediate next steps: 整合 `d2afe88e` 的 Review Bus 修复与本轮 UI Golden/截图粘贴修复；补齐 Tunnel 实际探测的 noauth 元数据路径；跑 Web、Swift CI、Review Bus、动态打包 App 验收；提交推送新的根候选；等待正式 GitHub Run；最后从该固定 Commit 原子替换 `/Users/apple/Applications/FilmOS Studio.app`，核对 SourceIdentity、稳定签名、单实例、服务、MCP/Tunnel，并以同一 CI Artifact 生成唯一逐字节一致 Handoff ZIP。
