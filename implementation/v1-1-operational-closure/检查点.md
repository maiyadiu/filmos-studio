# Codex Thread Handoff

Current objective: 完成 V1.1 双专家真实运行闭环最终纠偏。

Active workspace: `<LOCAL_WORKTREE>/wt-v1-1-dual-expert-operational-closure-v1`

What is already done: 固定基线 worktree 已创建；任务包完整读取；WP-00 至 WP-06 的本地实现已完成。Review Bus 已支持 Candidate A→B、原子 Findings、Consensus、Issue Task Package、完整 Evidence、一次性 Chrome 配对、Review Center、GitHub Run/Artifact 远程验证与 Live Trace；用户反馈弹窗可粘贴截图，章节可切换易读/Markdown。Codex Coordinator 已打包进 Local Runtime，并改为固定基线的独立可写 Git worktree；普通画布 Agent 仍保持只读/按请求授权。桌面安装器会在 Application Support 内建立无网络本地源码镜像，避免访问 Downloads 触发 TCC。

Locked decisions / must preserve: V1.1 Constitution；单一 Review Bus/Film Core/Agent Runtime/Candidate/Tool Broker；ChatGPT MCP 只读；写回必须用户手势；OpenAI 模型 API 与真实 Provider 费用均为 0；不修改 main；不创建 RC1 Tag。

Important files and paths: `services/filmos-review-bus`、`canvas-agent/src/brains/review-codex-coordinator.ts`、`canvas-agent/src/brains/review-worktree-manager.ts`、`cli/filmos-review`、`services/filmos-chatgpt-app`、`extensions/filmos-review-bridge`、`desktop/macos/scripts/prepare-review-source-repository`、`web/src/components/governance`、`web/src/pages/admin/review-center`、`acceptance`。

Completed verification: Review Bus 25/25；Review CLI 3/3；Chrome Bridge 6/6；反馈 UI；Codex Coordinator/Worktree 6/6；Canvas Agent production build；Web production build；Evidence Index、V1.1 静态 Gate、进程级自动化 Candidate A→B Gate 均 PASS。完整 `rc-local` 已在单一 receipt `20260831T142332Z-ecfc79a9b9f7-rc-local` 中全绿通过。GitHub Attempt 1 `33403136054` 真实暴露 Node 22 SQLite ExperimentalWarning 与 JSON artifact 同流时的解析缺陷；修复候选 `5039f944ea96b22ce2ad9fbbe1586f0eefee2111` 的 Attempt 2 `33404083234` 已在全新 macOS Runner 全量通过。自动风险路由候选 `4e9798fa5db6adbdd9b8356c31d0c4e17af1c600` 的 GitHub Run `33407138710` 已全量通过，并按硬规则替换稳定 App，Commit/Tree/Build 已同步。真实项目 `b3bb06d2f52c9eeff4da08bba05b6d02` 已创建旧 Fast Issue `FILMOS-ISSUE-d7c907f4-073d-4f0d-8fc0-fc041f706a8f`；新包现场反馈 `FILMOS-ISSUE-79e36199-aae2-49fc-9c09-6495d808f75c` 进一步暴露 macOS 原生白名单遗漏 `risk`、导致请求停留本机的真实桥接缺陷。现已把风险字段合同下沉到 `ReviewBusRuntimeContract`，只允许 Review Bus 已声明的布尔风险键、仍拒绝客户端直接指定 Lane，并补原生回归测试与自动化进程级 Core 分流断言。上述为本地/自动化证据，不冒充外部 Live Gate。

Open issues: 提交并推送原生桥接纠偏 Commit；GitHub CI；按硬规则再次重打包稳定 App；让本机持久草稿 `FILMOS-ISSUE-79e36199-aae2-49fc-9c09-6495d808f75c` 自动重放并验证 Core Lane/Codex Coordinator 激活；完成真实 Issue→Codex→ChatGPT→Candidate A→B 双专家 Roundtrip；生成固定 Commit 的 UI Golden；最终 CI Handoff 与用户 ZIP 逐字节一致。

Next exact steps: 提交原生桥接纠偏并推送 GitHub；CI 通过后再次重打包 App；自动重放当前现场 Core Issue，再执行需要用户 ChatGPT 四次显式点击的外部 Live Gate。

Do not do: 不使用模型 API；不执行外部付费/上传/项目创建；不改 main；不建 RC1 Tag；不以静态字符串检查冒充 Live Gate。
