# Codex Thread Handoff

Current objective: 完成 V1.1 双专家真实运行闭环最终纠偏。

Active workspace: `<LOCAL_WORKTREE>/wt-v1-1-dual-expert-operational-closure-v1`

What is already done: 固定基线 worktree 已创建；任务包完整读取；WP-00 至 WP-06 的本地实现已完成。Review Bus 已支持 Candidate A→B、原子 Findings、Consensus、Issue Task Package、完整 Evidence、一次性 Chrome 配对、Review Center、GitHub Run/Artifact 远程验证与 Live Trace；用户反馈弹窗可粘贴截图，章节可切换易读/Markdown。Codex Coordinator 已打包进 Local Runtime，并改为固定基线的独立可写 Git worktree；普通画布 Agent 仍保持只读/按请求授权。桌面安装器会在 Application Support 内建立无网络本地源码镜像，避免访问 Downloads 触发 TCC。原生风险字段桥接已恢复，Review Bus 现提供仅限本机主令牌的全项目待办发现，Codex Coordinator 无活动画布时也会以反馈自带项目 ID 和隔离 review canvas scope 自动接管。

Locked decisions / must preserve: V1.1 Constitution；单一 Review Bus/Film Core/Agent Runtime/Candidate/Tool Broker；ChatGPT MCP 只读；写回必须用户手势；OpenAI 模型 API 与真实 Provider 费用均为 0；不修改 main；不创建 RC1 Tag。

Important files and paths: `services/filmos-review-bus`、`canvas-agent/src/brains/review-codex-coordinator.ts`、`canvas-agent/src/brains/review-worktree-manager.ts`、`cli/filmos-review`、`services/filmos-chatgpt-app`、`extensions/filmos-review-bridge`、`desktop/macos/scripts/prepare-review-source-repository`、`web/src/components/governance`、`web/src/pages/admin/review-center`、`acceptance`。

Completed verification: Review Bus 25/25；Review CLI 3/3；Chrome Bridge 6/6；反馈 UI；Codex Coordinator/Worktree 专项回归；Canvas Agent production build；Web production build；Evidence Index、V1.1 静态 Gate、进程级自动化 Candidate A→B Gate 均 PASS。完整 `rc-local` 已在单一 receipt `20260831T142332Z-ecfc79a9b9f7-rc-local` 中全绿通过。GitHub Attempt 1 `33403136054` 真实暴露 Node 22 SQLite ExperimentalWarning 与 JSON artifact 同流时的解析缺陷；修复候选 `5039f944ea96b22ce2ad9fbbe1586f0eefee2111` 的 Attempt 2 `33404083234` 已在全新 macOS Runner 全量通过。自动风险路由候选 `4e9798fa5db6adbdd9b8356c31d0c4e17af1c600` 的 GitHub Run `33407138710` 已全量通过；原生桥接修复候选 `d91360fadeca65fd54033ae1078415b3e7e080f7` 的 GitHub Run `33409340391` 已全量通过，并按硬规则替换稳定 App。真实项目 `b3bb06d2f52c9eeff4da08bba05b6d02` 的反馈 `FILMOS-ISSUE-79e36199-aae2-49fc-9c09-6495d808f75c` 已由 App 重放进入 Review Bus `core / EVIDENCE_FROZEN`，事件链与完整证据包已冻结；随后现场又复现“无活动画布时协调器不发现项目反馈”的缺口，现已增加本机内部全项目发现和无画布回退回归测试。使用 Acceptance 专用 Python 的 Canvas Agent 完整串行套件单次通过：`407 pass / 0 fail / 5 platform skip`，包含真实 Film Core HTTP 两项和 Dreamina P0 竞态；Review Bus 仍为 `25/25`，V1.1 自动化预检与 Evidence Index 均 PASS。无画布协调候选 `94e5a7c088f66b74d60509ea2877d8a17e881e0d` 的 GitHub Run `33416050532` 已在全新 macOS Runner 全量通过。现场反馈又确认已安装 App 仍是 `d91360fa`；当前源码已把章节页改为真实“易读 / Markdown 原文”双模式，整本 TXT/Markdown 导入明确自动按章/集/幕拆分，反馈粘贴同时支持 `clipboardData.items` 与 `files` 以兼容 macOS WKWebView；前端专项 `14 pass / 0 fail` 与 production build 已通过。上述为本地/自动化证据，不冒充外部 Live Gate。

Open issues: 提交并推送当前 UI 使用纠偏 Commit；对该新 Commit 重跑 GitHub CI；按硬规则再次重打包稳定 App；验证整本导入、双模式与截图粘贴的 App 现场路径；验证本机已冻结反馈 `FILMOS-ISSUE-79e36199-aae2-49fc-9c09-6495d808f75c` 在无活动画布下自动激活 Codex Coordinator；完成真实 Issue→Codex→ChatGPT→Candidate A→B 双专家 Roundtrip；生成固定 Commit 的 UI Golden；最终 CI Handoff 与用户 ZIP 逐字节一致。

Next exact steps: 提交当前 UI 使用纠偏并推送 GitHub；新 Commit CI 通过后重打包并替换 App；完成三项 App 现场验证；让当前现场 Core Issue 由 Codex Coordinator 接管，再执行需要用户 ChatGPT 四次显式点击的外部 Live Gate。

Do not do: 不使用模型 API；不执行外部付费/上传/项目创建；不改 main；不建 RC1 Tag；不以静态字符串检查冒充 Live Gate。
