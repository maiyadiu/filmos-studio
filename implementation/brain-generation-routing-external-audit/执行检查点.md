# V2.4 生产接线纠偏执行检查点

## 当前目标与状态

- 目标：按外部独立验收 P0 任务包完成 V2.4 生产接线纠偏。
- 状态：WP-00 至 WP-06 已完成；真实 Candidate App 黑盒已通过，正在固定 Commit 与远程 Acceptance。
- 固定基线：`9ead75ba2afda248f77a9c916d29769852283abe`
- 基线 Tree：`44b7b3b8399a6a268c8084952cda4def650aebbb`
- 分支：`fix/v2-4-production-wiring-external-audit-v1`
- Worktree：当前仓库根目录。

## 锁定边界

- 不修改 `main`，不创建或移动 RC1 Tag。
- 不重写 V2.4 架构，不建立第二套 Composer、Provider Task Store 或 Candidate。
- 所有产品外部网络请求、素材上传、外部项目创建和外部费用为 0。
- Production Composition 只使用 Candidate Acceptance Project 专用本地 Mock Provider。
- 真实生成链必须经过 Route、Catalog、Prompt、Authorization、Guard、Budget、Broker、Provider Receipt、Candidate。
- 用户指出的基线后时间差修复必须核对并防止回归，重点为 ChatGPT Host、本机 Runtime、Dreamina CLI 和 App 源码同步安装。

## 已完成与证据

- 任务包全文已读取。
- 固定 Commit 与 Tree 已核对。
- 独立 worktree 已从固定 Commit 创建。
- 基线后 10 个桌面/Runtime 修复提交已逐项移植，保留 ChatGPT Host、本机 Runtime、Dreamina CLI 与 App 源码同步安装修复。
- Reference 合同、Brain 精确绑定、Engine Connection 真值、Route 优先级、Production Composer、Broker/Budget、Film Core 持久化、Mock Provider Receipt 与 Candidate 边界已接入同一条权威链。
- 自动 Production Composition 已证明：拒绝提交 0、批准提交 1、Candidate 1、Approved 0、重启重复提交 0、外部网络 0、外部费用 0。
- Candidate `FilmOS Studio.app` 已在 `FilmOS_Acceptance_Project` 完成真实 UI 黑盒：Hard Lock Reference 1；Broker 拒绝保持 Submit 0；重新预览并批准后 Candidate 为 QC Pending；重启后状态恢复且未重提。
- App 黑盒发现并修复三项真实接线缺陷：`fetch` 接收者丢失导致 Illegal invocation、已连接 Reference 未进入 Composer 计数、Broker 拒绝后旧 Proposal 被错误复用。

## 当前风险

- 外部真实 Provider、素材上传和费用路径未获授权，保持未执行；本任务只允许专用本地 Mock Provider。
- 正式交付仍须绑定干净固定 Commit、GitHub Acceptance Run 与唯一 Handoff ZIP。

## 下一步

1. 清理只属于浏览器黑盒工具的临时文件并完成本地回归。
2. 固化并推送新 Commit，在 GitHub `integration` 执行完整 FilmOS Acceptance。
3. 用固定 Commit 的 Receipt 与 Release Manifest 生成唯一 Handoff ZIP。
