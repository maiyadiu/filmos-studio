# ADR-023 Canonical Cost Lexical Format

Status: Accepted. Monetary amounts are non-negative canonical ASCII decimal strings with up to six fractional digits and no exponent, sign, whitespace, leading zeros or redundant trailing zeros. Arithmetic uses fixed-scale integers/decimals after lexical validation.
