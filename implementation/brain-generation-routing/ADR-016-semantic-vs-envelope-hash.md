# ADR-016 Semantic vs Envelope Hash

Status: Accepted. Semantic hashes cover explicit technical projections; envelope hashes cover the complete immutable snapshot excluding self-hash. Both use JCS v1, domain separation and SHA-256, with independent tests.
