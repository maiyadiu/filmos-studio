# ADR-013 External Paid Gate Semantics

Status: Accepted. Automated acceptance costs zero and never performs provider writes. Real completion/generation/upload/project creation requires explicit user authorization and produces a separately bound receipt.
