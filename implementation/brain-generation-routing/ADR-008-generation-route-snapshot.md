# ADR-008 Generation Route Snapshot

Status: Accepted. Editable route drafts become immutable snapshots with separate semantic and envelope hashes. Submit accepts only the authorized snapshot; edits create a new snapshot and make dependent authorization stale.
