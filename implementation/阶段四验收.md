# 第四阶段验收矩阵

> `PASSED_LOCAL` 只表示本地临时数据和真实代码闭环通过，不等于外部视频已生成、正式发布或生产迁移完成。

| 验收面 | 当前状态 | 通过条件 | 证据位置 |
| --- | --- | --- | --- |
| SceneTwinVersion | PASSED_LOCAL | 正式持久化；稳定坐标系、空间拓扑、Anchor/ViewFamily 与安全血缘 | Contracts、Core tests |
| CameraVersion | PASSED_LOCAL | 三机位独立版本；镜头/轴线/Anchor/hash 守卫 | Core、Golden C |
| BlockingVersion | PASSED_LOCAL | 脚/躯干/脸/视线/手/动作/道具链独立版本 | Core、Golden C |
| CompositionVersion | PASSED_LOCAL | 画幅、主体屏幕区、遮挡与安全区独立版本 | Core、Golden C |
| Production Canvas 正式创建 | PASSED_LOCAL | Human 显式确认；expected revision/hash；幂等唯一；重复 fail closed | Backend/Web tests、浏览器 |
| 空间精准 STALE | PASSED_LOCAL | 只命中声明的 SceneTwin/Camera/Blocking/Composition 依赖 | Core、Golden C |
| Previs/Video Candidate | PASSED_LOCAL | 本地投影/人工结果只进入 Candidate，不自动批准 | Golden C |
| Spatial Continuity QC | PASSED_LOCAL | RGB/Depth/Normal/ObjectID 血缘与跨镜空间连续性受审计 | Golden C |
| 重启与备份恢复 | PASSED_LOCAL | 重启/备份恢复保留事实、Stable ID、状态与审计 | Recovery tests |
| 事务故障与幂等 | PASSED_LOCAL | 中途失败零部分写入；同请求重放回执稳定 | Core/Host tests |
| 默认关闭/回滚 | PASSED_LOCAL | 关闭 Stage 4 Flag 后 Host 原流程可用且无新增 DOM/请求 | Web/Browser tests |
| 外部执行/真实迁移 | NOT_AUTHORIZED | Provider、上传、额度、发布、真实迁移写入均为 0 | 回执 |
