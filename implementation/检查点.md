# Codex Thread Handoff

Current objective:
第四阶段 Golden C 已完成本地集成验收。下一阶段按总计划进入第 22–28 天 Beta 硬化：Remote/Hybrid、DeepSeek Adapter、性能、恢复、迁移、上游兼容、桌面安装包与 Beta 验收；外部发布、真实迁移和签名动作仍需单独授权。

Active workspace:
`/Users/apple/Downloads/other/短剧/FilmOS Studio`

What is already done:
- Stage 4 代码提交：`deaa72c9`（空间版本与精准 STALE）、`1174d6e9`（Production Canvas 正式创建）、`dfa0440f`（真实 Golden C 与恢复）；范围提交 `5575e86f`。
- Film Core Schema `0.4.0`：23 paths 全部 implemented；SceneTwin/Camera/Blocking/Composition 独立 UUIDv4、version/hash、审计和精确 Impact 已落地；50 tests passed。
- 真实 Golden C：SceneTwin V1→V2、三套 Camera/Blocking/Composition、四类 pass 血缘、本地 Previs、三个 Video Candidate、精准 lighting STALE、重启、备份恢复、事务故障与幂等均通过；无 Mock 回退，外部调用 0。
- Production Canvas：默认关闭；Human confirmation、Project revision、SourceText hash、归属、重复历史和并发均失败关闭；Canvas/Link/Guard/Audit 原子创建。
- Playwright 真实浏览器：创建预演→Human 二次确认→created/revision/audit→刷新复用同一 Canvas→关闭 Flag；数据库记录保持 1/1/1/1，Host 项目与章节继续可用。
- Backend、Web 471/0、类型检查和生产构建通过；Contracts、Core 和 Golden 全部通过。Canvas Agent OpenAPI 同步与构建通过；全量有一个既有顺序依赖 Dreamina 测试失败，单独 3/3 通过且 Stage 4 未改该路径。

Locked decisions / must preserve:
- 影策是唯一 Host 主干；Canvas 只作投影，Film Core 是影视正式事实源。
- Film ID 由 Core UUIDv4 生成；SceneTwin/Camera/Blocking/Composition 不得合并回 Canvas JSON；version/hash、追加审计与 Candidate/Review/Approval 分离不可弱化。
- STALE 只沿声明的 dependency scope 精确传播；未映射变化进入 `unresolved_changes`，不得整图污染。
- J-cut 只允许 Human audio-only 例外，不能豁免画面连续性。
- Production Canvas 正式创建必须 Human 显式确认、revision/hash 守卫、唯一关联、事务审计；所有 Film Feature Flag 默认 false。
- 旧 editable Python 环境可能指向 `../wt-film-core`；验收 integration 时必须显式 `PYTHONPATH=<workspace>/film-core/src`，避免运行错误 worktree。
- 未经单独授权不得外部生成、上传、远程发布、迁移用户真实数据或消费额度。

Important files and paths:
- `implementation/阶段四.md`
- `implementation/阶段四验收.md`
- `implementation/test-reports/浏览器GoldenC.md`
- `implementation/INTEGRATION_STATUS.yaml`
- `implementation/PROGRAM_BOARD.yaml`
- `implementation/EVIDENCE_LEDGER.csv`
- `tests/film-golden/证据.md`
- `tests/film-golden/golden_c_real.py`
- `film-contracts/`
- `film-core/`
- `backend/internal/service/production_canvas.go`
- `web/src/film/canvas/production-canvas-api.ts`

Commands or services currently relevant:
- `cd film-core && PYTHONPATH=src /tmp/filmos-core-venv-02/bin/pytest -q`
- `python3 tests/film-contract/validate_contracts.py`
- `FILMOS_CORE_PYTHON=/tmp/filmos-core-venv-02/bin/python /tmp/filmos-core-venv-02/bin/python -m pytest -q tests/film-golden/test_golden_*.py`
- `bun test && bun run typecheck && bun run build`（在 `web`）
- 当前无 dev server、浏览器、Backend 或 Film Core 临时进程占用。

Open issues:
- Canvas Agent 全量回归存在一个 Dreamina 长驻 CLI 顺序依赖测试：全量 1 fail，隔离 3/3 pass；需在对应 Runtime Track 单独定位，不能归为 Stage 4 缺陷或静默忽略。
- Story UI 当前使用 Host 本地非正式快照，尚未接入正式 Core ScriptVersion 查询/人审命令提交。
- Host Asset UI 当前只投影项目 detail 已知字段，尚无正式 Host 资产详情查询与 Film Approval 操作面。
- Beta 硬化尚未开始：Remote/Hybrid 实链、DeepSeek Adapter、性能基线、生产级灾难恢复、用户数据迁移、最新上游候选、桌面签名安装包和 Beta 验收均未完成。

Next exact steps:
1. 锁定第五阶段 Beta 硬化范围、红线、验收矩阵和变更请求；区分可本地自动实施项与需授权的外部/真实数据动作。
2. 复核 Track 12 Remote/Hybrid、Agent 适配器、Track 11 迁移、Track 00 上游兼容和桌面 Track 当前事实，再确定增量实现，不复制 Host 能力。
3. 建立性能与恢复基线；使用合成数据/临时备份演练，不读取用户正式数据库。
4. 单独诊断 Canvas Agent Dreamina 顺序依赖门禁并提交最小修复或隔离证据，不修改 Golden C 状态。
5. 只有获得明确授权后，才运行真实迁移、外部 Provider、远端发布、签名安装包或生产开关。

Do not do:
- 不改稳定 ID，不把正式状态写进 Canvas JSON，不复制 Host Project/Shot/Asset/Task。
- 不把 prepared、generated、reviewed、approved 混成一个状态。
- 不将本地测试、白盒、预演或只读血缘称为外部生成完成。
- 不因一个顺序依赖测试删除/跳过原测试，也不把隔离通过伪装成全量通过。
- 不执行外部生成、上传、积分消费、远程发布或真实迁移；不删除、移动或改写用户现有数据及 Codex session 文件。
