# Codex Thread Handoff

Current objective:
第二阶段真实 Golden A 已完成；下一阶段进入 Golden B、Impact/STALE、Story Review UI 与本地资产管理，不越过外部执行授权边界。

Active workspace:
`/Users/apple/Downloads/other/短剧/FilmOS Studio`

What is already done:
- `integration` 已合入第二阶段范围、共享合同、Core 正式记录、实际 MCP 注册、Web Production Canvas 入口、真实 HTTP Golden 和 loopback CORS。
- Film Core Schema `0.2.0`：17 paths，16 implemented，1 planned；37 tests passed。
- 真实 HTTP Golden A：2 passed；离线 Mock 回归 4 passed；`fallback_mock_used=false`，`external_provider_calls=0`。
- 正式链覆盖 ScriptVersion → Human Script Lock/Decision → Director/Coverage → VisualLock/AssetBinding → Prompt/Provenance/Package → Manual Import/AttemptEvidence/Candidate → Continuity Review → Human Approval。
- MCP 已在共享 server 注册但默认关闭；默认/`canvasOnly` 不增加 Film 工具，显式开启只增加 5 个。
- 浏览器真实 Host 临时项目验证：入口开启、Sidecar CORS、缺失 production 关联安全预演、关闭 Flag 后 Film DOM 消失。
- Web 原生 471/0；Film 专项 78/0、228 assertions；Canvas Agent 338/0、5 个 Windows 专项 skipped；Backend、Migration 8/0、Desktop 14/0 通过。

Locked decisions / must preserve:
- Yingce 是唯一 Host 主干；Canvas 只作投影，Film Core 是影视正式事实源。
- Film ID 由 Core UUIDv4 生成；version/hash 守卫、追加审计、Candidate/Review/Approval 分离不可弱化。
- 所有 Film Feature Flag 默认 false；Agent 不能批准，Human Approval 必须绑定当前 Candidate hash。
- 未经单独授权不得外部生成、上传、远程发布、迁移用户真实数据或消费额度。

Important files and paths:
- `implementation/阶段二验收.md`
- `implementation/test-reports/浏览器GoldenA.md`
- `implementation/INTEGRATION_STATUS.yaml`
- `implementation/PROGRAM_BOARD.yaml`
- `implementation/EVIDENCE_LEDGER.csv`
- `tests/film-golden/证据.md`
- `film-contracts/`
- `film-core/`
- `canvas-agent/src/mcp-server.ts`
- `web/src/pages/projects/detail/production-entry.tsx`

Commands or services currently relevant:
- `cd film-core && PYTHONPATH=src /tmp/filmos-core-venv-02/bin/pytest -q`
- `python3 tests/film-contract/validate_contracts.py`
- `FILMOS_CORE_PYTHON=/tmp/filmos-core-venv-02/bin/python bun test`（在 `canvas-agent`）
- `bun test && bun run typecheck && bun run build`（在 `web`）
- 当前无 dev server 占用；浏览器、Backend 与 Film Core 临时进程均已关闭。

Open issues:
- `GET /impacts/{entityId}` 仍为唯一 `planned` operation；Cue/Section 精确传播未实现。
- Host 缺少带 expected revision/hash 的唯一 production canvas 正式创建 API；当前 UI 只安全预演。
- 浏览器验证覆盖入口/CORS/预演/Flag 回滚，不等于浏览器逐步操作完整正式对象链。
- ObjectID 仍为 `MISSING_NOT_IMPLEMENTED`；Flova 来源仍未验证；Comfy 具体能力仍需在线 heartbeat 证据。
- 未构建签名 `.app`；未执行完整灾难恢复、真实系统 A/B 数据迁移或 Candidate 分支发布验收。

Next exact steps:
1. Golden B：将 VisualLockSet 变化、资产替换与 Prompt/Candidate STALE 传播落到正式 Core，并实现只读 Impact API。
2. Story Review UI：接入 ScriptVersion/ScriptDecision/Cue 审核，保持 Web 只发预演/显式人审命令。
3. 本地资产与媒体：补 AssetVersion/Representation 检索、hash 绑定与安全预览，不上传。
4. Provider 血缘：完善 Dreamina/Comfy 本地回执与能力验证；任何真实生成仍需单独授权。
5. 系统 A/B：只读核查真实来源和清单，未授权前不执行正式迁移。

Do not do:
- 不改稳定 ID，不把正式状态写进 Canvas JSON，不复制 Host Project/Shot/Asset/Task。
- 不把 prepared、generated、reviewed、approved 混成一个状态。
- 不将本地测试、白盒或预演称为外部生成完成。
- 不删除、移动或改写用户现有数据及 Codex session 文件。
