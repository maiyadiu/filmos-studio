# Codex Thread Handoff

Current objective:
第五阶段 Beta 硬化已完成本地验收，状态为 `PASSED_LOCAL_WITH_KNOWN_BLOCKERS`。下一阶段应处理可本地实施的 Release Candidate 收口与 Web 拆包；真实 PG/用户迁移、Remote 执行、外部 Provider、签名公证和发布仍需独立授权。

Active workspace:
`/Users/apple/Downloads/other/短剧/FilmOS Studio`

What is already done:
- Stage 4 Golden C 完成：SceneTwin、Camera/Blocking/Composition 独立版本、精准 STALE、Production Canvas Human 原子创建和恢复通过。
- Stage 5 Remote：默认关闭的本地 Preview、Human confirmation、manifest version/hash、用户/项目隔离 receipt、幂等与 STALE 恢复已接入；真实网络执行/上传为 0。
- Stage 5 Agent：Codex/DeepSeek/Claude/Local/System/Human Only 使用同一 5-tool 面；Agent Apply、Approval、Lock 均失败关闭。Canvas Agent 354 tests 为 349 pass、0 fail、5 Windows skip。
- Stage 5 Migration：合成 6 表/14 行迁移包、row/FK/hash/Stable ID、幂等 receipt、事务故障、备份恢复和回滚通过；真实 PG 为 `BLOCKED_REAL_PG`，用户库未读取。
- Stage 5 Performance：Core/Remote/Agent/Browser p95 全部过本地预算；Web 有 10 个 >500 KB warning chunk，最大 1,821,047 bytes，未超过 2.5 MB 本地阻断线。
- Stage 5 Upstream：Stable `v1.2.1/61b33258...`；Candidate `19ebfbb3...` 原生 gate 通过，但 Models/Migrations 为 C，总体 `C_MIGRATION_REQUIRED`，未合并。
- Stage 5 Desktop：Swift 14/14；unsigned `.app` bundle/smoke 通过，hash `ea400550...`；未签名、公证或发布。
- Stage 5 Browser：本地 Remote 清单→Preview→Human receipt→刷新恢复→Flag off 通过；确认链无新增请求，Host 数据库无 Film/Remote 写入，最终 Console 0 error / 0 warning。
- 完整门禁：Contracts 23 implemented；Core 50；Golden Python 26；Golden TS 6/37；Backend 通过；Web 610/0、2583 assertions、typecheck/build 通过；Migration 10；Performance 2 个专项通过。

Locked decisions / must preserve:
- 影策是唯一 Host 主干；Film Core 是影视正式事实源，Canvas 只作投影。
- Film ID 由 Core UUIDv4 生成；版本/hash、追加审计、Candidate/Review/Approval 分离和精准 STALE 不得弱化。
- 正式 Apply/Canvas 创建必须 Human 显式确认和新鲜 revision/hash 守卫；Agent 不具备 Approval/Lock 权限。
- 所有 Film Feature Flag 默认 false；本地 cache/receipt 不代表服务器保存、Remote 发布或业务批准。
- 未经单独授权不得外部生成、上传、远端发布、读取/迁移用户真实数据库、消费额度、签名或公证。
- 验收 integration 时显式使用 `PYTHONPATH=<workspace>/film-core/src` 和 `FILMOS_CORE_PYTHON=/tmp/filmos-core-venv-02/bin/python`，避免 editable Python 指向旧 worktree。

Important files and paths:
- `implementation/阶段五.md`
- `implementation/阶段五验收.md`
- `implementation/test-reports/Beta验收.md`
- `implementation/test-reports/浏览器Beta.md`
- `implementation/INTEGRATION_STATUS.yaml`
- `implementation/PROGRAM_BOARD.yaml`
- `implementation/EVIDENCE_LEDGER.csv`
- `tests/film-beta/`
- `implementation/tracks/00-upstream/EVIDENCE.md`
- `implementation/tracks/08-agent/EVIDENCE.md`
- `implementation/tracks/11-migration/EVIDENCE.md`
- `implementation/tracks/12-remote/EVIDENCE.md`

Commands or services currently relevant:
- `python3 tests/film-contract/validate_contracts.py`
- `cd film-core && PYTHONPATH=src /tmp/filmos-core-venv-02/bin/pytest -q`
- `FILMOS_CORE_PYTHON=/tmp/filmos-core-venv-02/bin/python npm test`（在 `canvas-agent`）
- `bun test && bun run typecheck && bun run build`（在 `web`）
- `PYTHONPATH=film-core/src /tmp/filmos-core-venv-02/bin/python tests/film-beta/performance_local.py`
- `bun tests/film-beta/performance_surface.ts`
- 当前无 dev server、浏览器、Backend、Film Core 或临时数据库进程占用。

Open issues:
- Web 仍有 10 个大 chunk warning；下一阶段应先做路由/编辑器/媒体运行时拆包和真实冷启动复测。
- Candidate 上游仍为 `C_MIGRATION_REQUIRED`；不得因原生构建通过而直接合并。
- Remote 仍无真实执行器、Host 权限二次校验、部分失败/重试、上传和 publication receipt。
- 真实 PostgreSQL/用户迁移、Apple 签名/公证/发行、外部 Provider 和生产 Flag 均未授权。
- Story UI 与 Host Asset UI 仍主要是非正式/只读投影，正式查询与人审操作面需后续按合同扩展。

Next exact steps:
1. 锁定第六阶段 Release Candidate 范围和门禁，以本地 deterministic 收口为主，不自动扩大到外部发布。
2. 对 Web 最大 chunk 做归因与安全拆包，重跑 40 次浏览器读取、冷启动和全量 Web 门禁。
3. 把 Stage 5 本地迁移/恢复和 Remote receipt 纳入统一 RC 演练；继续保持真实数据与网络动作关闭。
4. 为上游 C 级 Models/Migrations 建立独立迁移适配计划；只有迁移、Golden 和回滚全部通过后才允许重新裁定 Candidate。
5. 获得明确授权后，才可单独启动临时真实 PostgreSQL、Remote executor、签名/公证或外部发布轨。

Do not do:
- 不改稳定 ID，不把正式状态写进 Canvas JSON，不复制 Host Project/Shot/Asset/Task。
- 不把 prepared、generated、reviewed、approved 混成一个状态。
- 不把本地 Preview、receipt、unsigned app 或候选原生构建称为生产发布完成。
- 不通过提高阈值、跳过测试或静默回退掩盖 Web chunk、迁移或上游阻断。
- 不执行外部生成、上传、积分消费、远程发布、真实迁移、签名或公证；不删除、移动或改写用户数据及 Codex session 文件。
