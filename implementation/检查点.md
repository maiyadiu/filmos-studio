# Codex Thread Handoff

Current objective:
实施 FilmOS Studio；开工里程碑已完成，下一里程碑是把当前默认关闭的本地首切片接成真实 Film Core / UI / MCP / Manual Provider Golden A 纵向链。

Active workspace:
`/Users/apple/Downloads/other/短剧/FilmOS Studio`

What is already done:
- 已完整读取 V6.1 总计划与 V6.2 开工总令。
- 已核查 GitHub 最新稳定 Release 为 `v1.2.1`。
- 已创建 `https://github.com/maiyadiu/filmos-studio`。
- 已建立 `origin`、`upstream-yingce`、`reference-tigerowo`、`reference-basket`。
- 本地 `main` 已指向 Release commit `61b332583c4fcbf71890ae67e3f0f104d67706b9`。
- 已读取根 `AGENTS.md`，并核查 Host 模型、数据库注册、Project API 和 MCP Schema。
- 已创建并推送 14 个独立 `track/*` 分支与 worktree。
- Track 00–13 均已启动；Track 00–13 的当前首切片/治理证据均已合入 `integration`，各轨分支和 worktree 保留。
- 已有上游兼容脚本、桌面核心、Film Core V0、项目/剧本/生产画布/资产/Prompt/Agent/Director/Provider/迁移/Remote 首切片、原生基线与 Golden A 离线 Mock。
- Film Core 已实现 8 条运行时接口、UUIDv4、六状态轴、乐观并发和原子追加审计；10 项测试通过。
- 当前集成头：Film Web 69 项通过，TypeScript 与生产构建通过；Canvas Agent 原生 328 项通过、5 项 Windows 专项跳过；迁移沙箱 8 项通过；桌面核心 14 项通过。
- Film Agent 未注册到实际 MCP；Provider 只准备 `not_submitted` 包并接受人工结果；Remote 只生成 `PREVIEW_ONLY` 清单；所有新入口仍默认关闭。

Locked decisions / must preserve:
- 产品名 FilmOS Studio，架构名 Film Production OS。
- Yingce Upstream 是唯一 Host 主干；不整仓合并 Tigerowo/Basket。
- Canvas 是投影，不是影视正式事实源。
- DirectorUnit 与 Shot 分离；生成成功只产生 Candidate，QC 与人审后才能 Approved。
- 未经单独授权不上传本地资产、不执行外部生成或消费积分。

Important files and paths:
- `implementation/PROGRAM_BOARD.yaml`
- `implementation/INTEGRATION_STATUS.yaml`
- `implementation/DAILY_REPORT.md`
- `implementation/检查点.md`
- `implementation/FILE_OWNERSHIP.yaml`
- `implementation/EVIDENCE_LEDGER.csv`
- `implementation/FIT_GAP_MATRIX.csv`
- `implementation/RISK_REGISTER.csv`
- `film-contracts/`
- `film-core/`
- `desktop/macos/`
- `web/src/film/`
- `tests/film-contract/`
- `tests/film-golden/`

Commands or services currently relevant:
- `git status --short --branch`
- `python3 tests/film-contract/validate_contracts.py`
- `cd web && bun test <8 Film targets> && bun run typecheck && bun run build`
- `cd film-core && /tmp/filmos-core-venv-02/bin/pytest tests`
- 尚未启动任何 dev server，尚未占用端口。

Open issues:
- `candidate` 只是分支占位，尚未通过 Golden，不是候选版验收结果。
- upstream `main` 当前是静态 C/原生验证 D，不得合入。
- 共享 OpenAPI 中 4 条目标路径仍是 `planned`，没有运行时实现。
- 当前通过的是本地领域/投影/离线网关/Mock 门禁，不是浏览器和真实 Provider 纵向验收。
- ObjectID 为 `MISSING_NOT_IMPLEMENTED`；Flova 为 `UNVERIFIED_SOURCE_ABSENT/DEFERRED`；Comfy 具体能力尚需在线 heartbeat 证据。
- 系统 A/B 真实数据未读取，迁移仅验证 marker-bound sandbox；Remote 没有 UI/API/执行器接线。

Next exact steps:
1. 审核 `implementation/CHANGE_REQUESTS/`，优先接受 Prompt/Agent/Director/Provider 的共享合同与生成式 MCP Schema 门禁。
2. 用真实 Film Core 与实际注册的只读/Preview MCP 替换 Golden A 对应 Mock 层；Manual Provider 仍只使用本地 fixture/人工导入，不做外部生成。
3. 接 Project UI/Production Canvas 的默认关闭入口，运行浏览器 Golden A、冲突、恢复与回滚测试。
4. 真实 Provider、上传、Remote 发布、系统 A/B 迁移均需用户另行授权后再执行。

Do not do:
- 不改稳定 ID，不换画布引擎，不复制 Host 已有 Project/Shot/Asset/Task。
- 不将影视正式状态写入 Canvas JSON。
- 不删除或改写用户现有数据。
