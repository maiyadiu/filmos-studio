# Codex Thread Handoff

## Current checkpoint — fixed Acceptance Project and remote handoff

Current objective:
持续建设独立外部验收所需的 `acceptance/` 原始证据；当前已实现固定项目同一 Sidecar Local-first 全链，正在把新检查纳入 RC Local 证据。`filmos-v1.0.0-rc1` 尚未冻结。

Active workspace and state:
- 工作区为 `$REPO`，分支 `integration`。
- 当前开发基线 Commit 为 `40b5c0d7e0aee902767dc23cc51538c9ce8538db`，工作树包含尚未提交的桌面版、品牌、本地免登录和验收体系改动。
- 唯一正式远程验收仓库是 `maiyadiu/filmos-studio`；已核对 `origin` fetch/push 皆指向该仓库。
- 已安装 App 为 `$HOME/Applications/FilmOS Studio.app`；源码只用于开发/重建，App 运行时不得打开源码、Downloads 或 Bun。
- 本地正式数据为 `~/Library/Application Support/FilmOS Studio/WorkbenchData`；WebKit 缓存不是正式备份源。

Locked decisions:
- 本地免登录仅允许显式 `127.0.0.1` 桌面运行时；公开线上版继续使用账号登录。
- 备份只是用户显式动作。启动不请求 Downloads/源码目录授权，不修改或规避 macOS TCC。
- `.filmosbackup` 必须包含 SQLite 一致性快照、资源文件和逐文件 SHA-256 manifest；不得包含 `.settings-key`、API Key、Cookie、CLI 登录凭据和 WebKit 可重建缓存。
- Film Core 仍是正式事实源；Canvas/ChatGPT/Agent 的 Candidate、Preview 或 succeeded 不能替代 QC、Approved、Lock 或 Formal Apply。
- 冻结 RC 前必须在干净工作树完整重跑 `acceptance/run_all`；冻结 Commit/Tag 不得移动，修复进入下一 RC。
- Pre-RC 必须在 `origin` 推送明确的候选 ref 和完整 Commit SHA；正式 RC 必须推送不可变 `filmos-v1.0.0-rc1`，外部验收不跟随漂移中的 `main`。
- `acceptance/MANIFEST.json` 只记录稳定验收规范、Evidence Index/Golden Project Hash、Yingce upstream ref 和 Film Core/Canvas/MCP Schema 版本，不记录自身 Commit SHA。固定 Commit 后由 Runner/CI 生成 artifact-only `RELEASE_MANIFEST.json`绑定 Commit、Tree、Receipt 和 Build，不回写该 Commit。

Completed and verified:
- macOS App 已改为内置 `FilmOSBackend`、`FilmOSWeb` 与 `web/dist`，运行时配置不含源码、Downloads、Bun 或用户绝对路径。
- 后端已实现 `GET /api/desktop/backup`：仅桌面本地认证和显式 IPv4 loopback 可用，使用 SQLite `VACUUM INTO` 建立一致性快照。
- 桌面菜单已接入“打开 FilmOS 数据目录”和“导出 FilmOS 备份包…”；Web 桥在导出前刷新当前画布、Canvas/Asset 持久化与后端存储。
- `DESKTOP-BACKUP-RESTORE-001` 已通过真实临时后端 HTTP 演练：写入 Golden 项目、导出、验证 ZIP/manifest/hash/隐私排除、还原到新目录、重启并读回同一项目。
- `ACCEPTANCE-FULL-CHAIN-001` 已独立通过：同一真实 Sidecar 内完成全链、人类审批、精准 STALE 与重启原样读回；fallback=false、external_provider_calls=0。
- `REMOTE-ACCEPTANCE-CONTRACT-001` 已独立通过开发态合同：当前不冒充 Pre-RC/RC，且 origin fetch/push 均是锁定仓库。
- `.github/workflows/acceptance.yml` 已接入 macOS 干净环境 `acceptance/run_all` 与原始证据 artifact。
- `PERFORMANCE-LOCAL-001` 已通过：80 ContentUnit/80 Shot/60 samples 下 Core、Remote、Agent 全部低于 p95 预算；Agent Apply、网络、上传和外部 Provider 均为 0；Web 有 10 个 warning chunk、无 blocking chunk。
- 6 份验收报告和 `acceptance/reports/REPORT_INDEX.json` 已由原始 receipt/log/source hash 生成；`ACCEPTANCE-REPORT-BUNDLE-001` 已反向校验 35 个证据对象。
- 后端 Go 测试、Web TypeScript、Swift release build、桌面品牌检查与验收隐私扫描已通过。
- 最近的全量开发 receipt 为 `acceptance/evidence/runs/20260828T152423Z-40b5c0d7e0ae-rc-local/receipt.json`，receipt 内嵌规范 SHA-256 为 `d3c787598a832a0ba5d0363daae1433ceb8ddb067e76e038c14f46b02c0e575c`；20/20 检查全通过。所有 Track 的 local required checks 已无缺项，总状态为 `DEVELOPMENT_LOCAL_EVIDENCE_PASSED_DIRTY`，仍不是 Pre-RC 或冻结 RC。

Important files:
- `acceptance/合同.json`、`acceptance/EVIDENCE_INDEX.json`、`acceptance/FilmOS_Acceptance_Project/项目.json`
- `acceptance/MANIFEST.json`、`acceptance/checks/full_chain.py`、`acceptance/checks/remote_contract.py`
- `acceptance/generate_reports.py`、`acceptance/checks/reports.py`、`acceptance/reports/REPORT_INDEX.json`
- `.github/workflows/acceptance.yml`
- `acceptance/run.py`、`acceptance/run_all`、`acceptance/verify.py`、`acceptance/checks/desktop_backup_restore.py`
- `backend/internal/service/desktop_backup.go`、`backend/internal/handler/desktop_backup.go`
- `desktop/macos/Sources/FilmOSStudioDesktop/main.swift`
- `web/src/lib/desktop-backup-bridge.ts`

Open risks and exact next steps:
1. 当前工作树未提交，新 receipt 只能登记为 `DEVELOPMENT_EVIDENCE_DIRTY`，不能冻结 RC。
2. 整理当前工作树为可审核 Commit，在该固定 Commit 的独立干净检出中重跑 `acceptance/run_all`，生成不纳入 Git 的 `RELEASE_MANIFEST.json`。
3. 只有干净证据通过后才推送 Pre-RC Candidate 到 `maiyadiu/filmos-studio`并输出完整 SHA；本阶段不创建 RC1 Tag。
4. 外部 ChatGPT acknowledgement、真实 Provider CLI 生成、真实用户库迁移/回滚保持明确阻断，不用 mock 冒充；全部齐备后才能冻结 RC。
5. 当前 Command Line Tools 缺少 Swift `Testing` module；现有 `swift test` 套件在该工具链不能运行，release build 可通过，不得把此项写成通过。
6. 外部 ChatGPT acknowledgement、真实 Provider/CLI 生成、真实用户库迁移、签名、公证和公开发布仍需新授权或外部条件。

Do not do:
- 不删除或覆盖用户工作树、真实数据库、源码目录或 Codex session。
- 不把 README、总结 Markdown、health check、代码完成或本地 mock 当作正式验收。
- 不把密钥、Cookie、CLI 凭据或本机隐私绝对路径写入验收包。

## Previous stage checkpoint

Current objective:
第六阶段 Release Candidate 本地收口与 Track 14 Day 1–5 已完成。当前状态为 `PASSED_LOCAL_RC_WITH_EXTERNAL_BLOCKERS`；外部 ChatGPT/Tunnel、真实数据库迁移、上游合并、签名与发布仍需独立授权。

Active workspace:
`$REPO`

What is already done:
- Stage 4 Golden C：SceneTwin、Camera/Blocking/Composition 独立版本、精准 STALE、Production Canvas Human 原子创建与恢复通过。
- Stage 5 Beta：Remote 本地 Preview/receipt、通用 Agent Adapter、合成迁移、性能、unsigned app 与浏览器回退通过。
- Stage 6 Web：9 个交互弹层 lazy load；Project chunk 1,294,180 降至 873,602 B（-32.50%）；10 个 warning 不增；最大 1,821,047 B；阈值未提高。
- Stage 6 RC recovery：统一 runner 串联 Core、Remote、Migration、Agent/session、20 个 Flag 与上游适配；11/11 checks，来源/Stable ID 不变。
- Upstream C adapter：基于 Candidate 19ebfbb3 的真实 diff，合成 3 新表、ChannelModel.Icon 与 14 项 migration 增量；可逆 dry-run/故障回滚/exact restore 通过，裁定仍为 C_MIGRATION_REQUIRED，未合并。
- Track 14 Day 1–5：Tool Contract、20 个公开工具、7 Widgets、Project Grant/审计、loopback MCP、媒体代理、Proposal export/import Preview、Web Handoff、macOS Keychain/文件打开、插件、兼容快照、恢复说明均已实现。
- Track 14 real Golden：真实 Film Core SQLite/HTTP → MCP → Widget，以及真实 .filmosproposal → Python CLI Preview/幂等通过；fallback_mock_used=false、external_calls=0。
- 安全收口：7 个 chatgpt Flag 全 false；预留写工具注册 0；Python Preview 子进程只接收 4 个白名单环境值。
- 主工作树门禁：Contracts 5；MCP 22；Importer 21；Track14 Golden 1；Film Core 50；Golden Python 26；Golden TS 6/37；Backend pass；Web 617/0；Canvas Agent 349 pass/5 skip；Desktop 27/0；RC Recovery 4 + Surface 1/20 + Migration 17；Plugin validator pass。

Locked decisions / must preserve:
- 影策是唯一 Host 主干；Film Core 是影视正式事实源，Canvas 与 ChatGPT 都不是事实源。
- Film 稳定 ID、版本/hash、追加审计、Candidate/Review/Approval 分离和精准 STALE 不得弱化。
- 正式 Apply/Canvas 创建必须 Human 显式确认和新鲜 revision/hash 守卫；ChatGPT/Agent 不具备 Approval/Lock 权限。
- `.filmosproposal` 导入最多产生 Proposal、Candidate 或 Review Draft；不得产生 Approved、Locked、Formal Apply、Provider Task 或 Delete。
- 所有 Film 与 Track 14 Feature Flag 默认 false；本地 cache/receipt/doctor 不代表服务器保存、ChatGPT 连接或业务批准。
- 未经单独授权不得外部生成、上传、远端发布、读取/迁移用户真实数据库、消费额度、签名或公证。

Important files and paths:
- `implementation/阶段六.md`
- `implementation/阶段六验收.md`
- `implementation/INTEGRATION_STATUS.yaml`
- `implementation/PROGRAM_BOARD.yaml`
- `implementation/FEATURE_FLAGS.yaml`
- `implementation/EVIDENCE_LEDGER.csv`
- `services/filmos-chatgpt-app/EVIDENCE.md`
- `services/filmos-chatgpt-app/使用与恢复.md`
- `services/filmos-chatgpt-app/evidence/real-golden-receipt.json`
- `services/filmos-chatgpt-app/evidence/external-account-blocked.json`
- `tests/film-rc/rc-recovery-receipt.json`
- `implementation/tracks/14-chatgpt/Web交接验收.md`
- `implementation/tracks/14-chatgpt/桌面接线.md`

Commands or services currently relevant:
- `python3 tests/film-contract/validate_contracts.py`
- `cd film-core && PYTHONPATH=src /tmp/filmos-core-venv-02/bin/pytest -q`
- `FILMOS_CORE_PYTHON=/tmp/filmos-core-venv-02/bin/python npm test && npm run build` in `canvas-agent`
- `bun run test && bun run typecheck && bun run build:audit` in `web`
- `npm test && npm run check && npm run build && npm run doctor` in `services/filmos-chatgpt-app`
- `FILMOS_TEST_PYTHON=/tmp/filmos-core-venv-02/bin/python npm run test:golden-real`
- `scripts/recovery/RC恢复演练 --synthetic --output <new-path>`
- 当前没有需要保留的 dev server、Tunnel、外部连接或真实数据库进程。

Open issues:
- ChatGPT Developer Mode 安装、实际 Tool Scan、Secure MCP Tunnel 与外部账号 acknowledgement 尚未执行，状态必须保持 BLOCKED_EXTERNAL_ACCOUNT。
- Web 仍有 10 个 >500 KB warning chunk；Project 主块已达 RC 预算，但 Excalidraw/语言包等重依赖仍可在后续继续拆。
- Upstream Candidate 仍为 C_MIGRATION_REQUIRED；真实用户 SQLite/PostgreSQL 未打开，Candidate 未合并。
- Remote 仍没有获授权的真实执行/上传/publication receipt。
- Apple 签名、公证、发行包与生产发布未授权。

Next exact steps:
1. 如用户授权外部 ChatGPT 接入，再单独执行 Developer Mode、Secure MCP Tunnel、Tool Scan 和接收端 acknowledgement；不得把本地 doctor 当外部证据。
2. 如用户授权真实迁移验证，先指定隔离临时 PostgreSQL/测试数据库与回滚窗口；仍不得接触用户生产库。
3. 只有真实迁移、Golden、回滚与批准齐全后，才重新裁定并可能吸收 upstream Candidate。
4. 签名、公证、DMG/PKG 与公开发布必须作为独立发行阶段。
5. 若继续纯本地开发，先定义第七阶段范围，不自动打开上述外部轨。

Do not do:
- 不改稳定 ID，不把正式状态写进 Canvas JSON，不复制 Host Project/Shot/Asset/Task。
- 不把 prepared、local ready、connected、generated、reviewed、approved 混成一个状态。
- 不把本地 MCP、Preview、receipt、unsigned app、合成迁移或候选构建称为生产发布完成。
- 不通过提高阈值、跳过测试、公开 localhost 或静默回退掩盖阻断。
- 不执行外部生成、上传、积分消费、远程发布、真实迁移、上游合并、签名或公证；不改写用户数据或 Codex session 文件。
