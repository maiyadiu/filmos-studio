# Codex Thread Handoff

Current objective:
第三阶段 Golden B 与精准 STALE 已完成本地集成验收；下一阶段进入 Golden C、SceneTwin/镜头版本化、Production Canvas 正式创建边界和恢复演练，不越过外部执行授权边界。

Active workspace:
`/Users/apple/Downloads/other/短剧/FilmOS Studio`

What is already done:
- `integration` 的第三阶段实现代码头为 `bac96e10`；本检查点与验收账本固化该代码头的全量门禁结果。
- Film Core Schema `0.3.0`：21 paths 全部 implemented；`ScriptStructureMap`、`ImpactEdge`、Impact 查询与原子精准 STALE 已落地；44 tests passed。
- 真实 Golden B：三人长对白、DirectorUnit/Shot 多对多、五维连续性、受限 J-cut、Character/Costume Approved Lock、Cue/Costume 精准传播、unresolved change、冲突零部分写入和幂等重放均通过；无 Mock 回退，外部调用 0。
- Story Review 与 Host Asset 只读投影均默认关闭；真实浏览器完成启用/关闭双态复核，Host 原流程保持可用。
- Dreamina 仅新增默认关闭的只读影视血缘投影，没有提交、上传、批准或消耗额度。
- Web 全量 602/0、2545 assertions；Film 专项 97/0、261 assertions；Canvas Agent 343/0、5 个 Windows 专项 skip；Backend、Migration 8/0、类型检查和构建均通过。

Locked decisions / must preserve:
- 影策是唯一 Host 主干；Canvas 只作投影，Film Core 是影视正式事实源。
- Film ID 由 Core UUIDv4 生成；稳定 Cue/Section、version/hash 守卫、追加审计、Candidate/Review/Approval 分离不可弱化。
- STALE 只沿声明的 dependency scope 精确传播；未映射变化进入 `unresolved_changes`，不得整图污染。
- J-cut 只允许 Human audio-only 例外，不能豁免画面连续性。
- 所有 Film Feature Flag 默认 false；Agent 不能批准，Host confirmed 不等于 Film Approved。
- 未经单独授权不得外部生成、上传、远程发布、迁移用户真实数据或消费额度。

Important files and paths:
- `implementation/阶段三验收.md`
- `implementation/test-reports/浏览器GoldenB.md`
- `implementation/INTEGRATION_STATUS.yaml`
- `implementation/PROGRAM_BOARD.yaml`
- `implementation/EVIDENCE_LEDGER.csv`
- `tests/film-golden/证据.md`
- `tests/film-golden/golden_b_real.py`
- `film-contracts/`
- `film-core/`
- `web/src/film/story/`
- `web/src/film/assets/`

Commands or services currently relevant:
- `cd film-core && PYTHONPATH=src /tmp/filmos-core-venv-02/bin/pytest -q`
- `python3 tests/film-contract/validate_contracts.py`
- `FILMOS_CORE_PYTHON=/tmp/filmos-core-venv-02/bin/python bun run test`（在 `canvas-agent`）
- `bun test && bun run typecheck && bun run build`（在 `web`）
- 当前无 dev server、浏览器、Backend 或 Film Core 临时进程占用。

Open issues:
- SceneTwin、独立 `CameraVersion`、`BlockingVersion`、`CompositionVersion` 尚未实现；Golden C 未运行。
- Host 缺少带 expected revision/hash 的唯一 Production Canvas 正式创建 API；当前仍是安全预演。
- Story UI 当前使用 Host 本地非正式快照，尚未接入正式 Core ScriptVersion 查询/人审命令提交。
- Host Asset UI 当前只投影项目 detail 已知字段，尚无正式 Host 资产详情查询与 Film Approval 操作面。
- 完整灾难恢复、签名 `.app`、真实系统 A/B 数据迁移和 Candidate 发布验收未执行。

Next exact steps:
1. 锁定 Golden C 规格与 SceneTwin/Camera/Blocking/Composition 正式合同，先更新变更请求和验收矩阵。
2. 在 Film Core 实现对应版本记录、六轴状态、依赖边和 Impact 传播，不修改 Host 核心表。
3. 为 Production Canvas 唯一正式创建设计 expected revision/hash + Human confirmation + Audit 回执；默认关闭。
4. 补 Core 重启/数据库备份恢复/中途故障恢复和 UI 回滚矩阵。
5. 真实临时 Sidecar + 浏览器运行 Golden C；外部 Provider 仍只做本地 fixture/血缘验证。

Do not do:
- 不改稳定 ID，不把正式状态写进 Canvas JSON，不复制 Host Project/Shot/Asset/Task。
- 不把 prepared、generated、reviewed、approved 混成一个状态。
- 不将本地测试、白盒、预演或只读血缘称为外部生成完成。
- 不执行外部生成、上传、积分消费、远程发布或真实迁移；不删除、移动或改写用户现有数据及 Codex session 文件。
