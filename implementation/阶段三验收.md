# 第三阶段验收矩阵

> 代码/测试状态与业务批准状态分开；`PASSED` 不等于外部生成或发布完成。

| 验收面 | 当前状态 | 通过条件 | 证据位置 |
| --- | --- | --- | --- |
| ScriptStructureMap | NOT_RUN | companion 绑定当前 ScriptVersion；Cue/Section 稳定且不复制正文 | `film-contracts/`、Core tests |
| Impact 查询 | NOT_RUN | `/impacts/{entityId}` 实际实现；入/出边与 scope 可追溯 | OpenAPI、Core tests |
| 精准 STALE | NOT_RUN | 只命中声明依赖；stale_state 分轴；事务、幂等、环和 stale guard | Core tests、Golden B |
| Story Review UI | NOT_RUN | 默认关闭；版本/hash/差异/影响可见；Web 不自动正式写 | Web tests、浏览器 |
| Asset/Media UI | NOT_RUN | Host 只读投影；canonical containment；无复制、URL/data URL、上传 | Web tests、浏览器 |
| 多对多 Coverage | NOT_RUN | DirectorUnit 与 Shot 非 1:1，绑定当前锁定剧本 | Golden B |
| 连续性/J-cut | NOT_RUN | axis/eyeline/blocking/action/prop 检查；J-cut 例外范围受限 | Core/Golden B |
| Character/Costume Lock | NOT_RUN | Approved binding + VisualLock dependency key；替换精准影响 | Core/Golden B |
| 真实 Golden B | NOT_RUN | 真 Sidecar/SQLite/HTTP，无 Mock fallback，冲突恢复通过 | `tests/film-golden/` |
| 默认关闭/回滚 | NOT_RUN | 关闭 Story/Asset/Core Flag 保持 Host 原流程 | Web/Browser tests |
| Provider/系统 A/B | NOT_AUTHORIZED | 只允许本地血缘/只读 fixture；网络、上传、额度和迁移写入为 0 | 回执 |
