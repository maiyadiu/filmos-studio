# 第三阶段验收矩阵

> 代码/测试状态与业务批准状态分开；`PASSED` 不等于外部生成或发布完成。

| 验收面 | 当前状态 | 通过条件 | 证据位置 |
| --- | --- | --- | --- |
| ScriptStructureMap | PASSED | companion 绑定当前 ScriptVersion；Cue/Section 稳定且不复制正文 | Core 44 pass、Golden B |
| Impact 查询 | PASSED | `/impacts/{entityId}` 实际实现；入/出边与 scope 可追溯 | OpenAPI 0.3.0、Core tests |
| 精准 STALE | PASSED | 只命中声明依赖；stale_state 分轴；事务、幂等、环和 stale guard | Core tests、真实 Golden B |
| Story Review UI | PASSED_LOCAL | 默认关闭；版本/hash/差异/影响可见；Web 不自动正式写 | Web tests、浏览器 Golden B |
| Asset/Media UI | PASSED_LOCAL | Host 只读投影；canonical containment；无复制、URL/data URL、上传 | Web tests、浏览器 Golden B |
| 多对多 Coverage | PASSED | DirectorUnit 与 Shot 非 1:1，绑定当前锁定剧本 | 真实 Golden B |
| 连续性/J-cut | PASSED | axis/eyeline/blocking/action/prop 检查；J-cut 例外范围受限 | Core/Golden B |
| Character/Costume Lock | PASSED | Approved binding + VisualLock dependency key；替换精准影响 | Core/Golden B |
| 真实 Golden B | PASSED_LOCAL | 真 Sidecar/SQLite/HTTP，无 Mock fallback，冲突恢复通过 | `tests/film-golden/` |
| 默认关闭/回滚 | PASSED | 关闭 Story/Asset/Core Flag 保持 Host 原流程 | Web/Browser tests |
| Provider/系统 A/B | NOT_AUTHORIZED | 只允许本地血缘/只读 fixture；网络、上传、额度和迁移写入为 0 | 回执 |

## 验收结论

第三阶段本地集成验收通过。`PASSED_LOCAL` 只表示临时数据库、真实 Sidecar、项目源码和真实浏览器组成的本地闭环通过，不提升为生产批准、外部生成完成或正式迁移完成。

下一阶段转入 Golden C：SceneTwin、独立 Camera/Blocking/Composition 版本、Production Canvas 正式创建边界与恢复演练；在另行授权前，外部执行面继续关闭。
