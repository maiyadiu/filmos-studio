# 第六阶段验收矩阵

## 总结论

第六阶段状态为 `PASSED_LOCAL_RC_WITH_EXTERNAL_BLOCKERS`。Release Candidate 本地收口与 Track 14 Day 1–5 均已完成确定性实现和主工作树复验；最高状态仍只到本地 RC，不代表 ChatGPT 外部账号已连接、生产数据库已迁移、上游 Candidate 已合并或应用已发布。

## 验收矩阵

| 验收面 | 状态 | 当前证据 |
| --- | --- | --- |
| 官方能力核查 | `VERIFIED` | MCP Server、MCP Apps bridge、Auth、metadata snapshot 与 Secure MCP Tunnel 边界已按官方文档固定 |
| Tool Contract v1 | `PASSED_LOCAL` | 单一 JSON 生成 TS/MCP/OpenAPI；5 tests；合同 hash `70018bc1...b54bb` |
| 标准与 FilmOS 工具 | `PASSED_LOCAL` | 2 个标准工具、10 个只读数据工具、7 个 render 工具、1 个 Proposal artifact；公开工具 20，预留写工具注册 0 |
| Project Grant/Auth | `PASSED_LOCAL` | 单项目、短期、撤销/过期/跨项目拒绝；原始 token 不落盘，审计不含路径或凭据 |
| MCP Apps Widget | `PASSED_LOCAL` | Project、ContentUnit、Shot 及 Asset、SceneTwin、Review、Proposal 共 7 个资源；官方 `ext-apps` bridge 优先 |
| Proposal Handoff | `PASSED_LOCAL` | 1 MiB；HMAC、过期、project/state/version/hash、非法命令/脚本/URL/路径与幂等校验；只返回 Preview |
| 桌面文件关联/导入 | `PASSED_LOCAL_UNSIGNED` | `.filmosproposal` UTType、App open event、Keychain、固定 Core Preview CLI；27 tests / 6 suites；无 Apply |
| Web Handoff UI | `PASSED_LOCAL` | Flag off 为 0 DOM/0 read/0 request；内存 Grant；本地/外部状态分离；桌面与 390×844 浏览器复核通过 |
| Prompt Injection/数据安全 | `PASSED_LOCAL` | MCP 输出脱敏、项目文本不提权、媒体代理有项目边界；Python Preview 子进程只接收 4 个白名单环境变量 |
| Tool Snapshot兼容 | `PASSED_LOCAL` | 冻结 v1 snapshot replay；删除或收窄合同标记 migration required |
| Golden ChatGPT A/B | `PASSED_LOCAL_REAL` | 真实 Film Core SQLite/HTTP → MCP → Widget；真实文件 → Python CLI Preview；1 test；无 mock、外部调用 0、幂等通过 |
| Web 拆包 | `PASSED_LOCAL_RC` | Project chunk `1,294,180 → 873,602 B`，下降 32.50%；10 个 warning 不增；最大仍为 Excalidraw `1,821,047 B`；未提高阈值 |
| 统一 RC 恢复 | `PASSED_LOCAL_RC_RECOVERY` | 11/11 checks；20 个 Flag 全回退；Core/Remote/Migration/Agent/session/Stable ID/来源不可变均通过 |
| 上游 C 级迁移适配 | `PASSED_SYNTHETIC_REVERSIBLE_DRY_RUN` | 实际 Candidate diff、3 新表 + ChannelModel.Icon、14 项 migration 增量、故障回滚与 exact backup restore 通过 |
| 上游裁定 | `C_MIGRATION_REQUIRED` | 真实用户 SQLite/PostgreSQL 未打开、Candidate 未合并；合成演练不自动提升等级 |
| Secure Tunnel / ChatGPT账号 | `BLOCKED_EXTERNAL_ACCOUNT` | `tunnel_started=false`、`public_listener_created=false`；缺 Platform tunnel id/runtime key/外部账号回执 |
| 外部发布/真实数据动作 | `NOT_AUTHORIZED / NOT_EXECUTED` | Provider、上传、额度、公开监听、真实迁移、上游合并、签名、公证与发布均为 0 |

## 主工作树门禁

| 层 | 结果 |
| --- | --- |
| Film Contracts | `schema=0.4.0 paths=23 implemented=23 planned=0 axes=6` |
| Film Core | 50 pass；仅 Starlette 测试依赖弃用告警 |
| Golden A/B/C | Python 26 pass；TypeScript 6 pass / 37 assertions |
| Backend | `go test ./...` 全部通过 |
| Web | `bun run test` 617 pass / 0 fail / 2617 assertions / 85 files；typecheck、production build、bundle audit 通过 |
| Canvas Agent | 354 tests：349 pass / 0 fail / 5 Windows skip；build 通过 |
| Track 14 Contract | 5 pass；check/build 通过 |
| Track 14 MCP | 22 pass；check/build/doctor 通过；公开 20、写工具 0 |
| Track 14 Importer | 21 pass |
| Track 14 real Golden | 1 pass；`fallback_mock_used=false`、`external_calls=0` |
| RC recovery/migration | Surface 1 pass / 20 assertions；Recovery 4 pass；Migration 17 pass；receipt file SHA-256 `08db8c15...3991a` |
| Desktop | 27 tests / 6 suites；release 与 unsigned app verify 通过；`services_started=false` |
| Performance | Core app/context/read/preview、Remote、Agent p95 均过预算；Agent Apply 0、网络/上传/Provider 0 |
| Plugin | `plugin-creator` validator 通过 |

## 关键回执

- `tests/film-rc/rc-recovery-receipt.json`：receipt id `6407b984-9c80-509e-8dcc-1efde5156a78`，内部 receipt hash `38be0733...28cc`，20 个 Flag，11/11 checks。
- `services/filmos-chatgpt-app/evidence/real-golden-receipt.json`：真实 SQLite/HTTP/MCP/Widget/文件/CLI，Proposal Preview 幂等且 `formal_write_executed=false`。
- `services/filmos-chatgpt-app/evidence/external-account-blocked.json`：外部账号与 Tunnel 仍阻断，无公开 listener。
- `implementation/tracks/14-chatgpt/Web交接验收.md`、`桌面接线.md`：Web 与 macOS 本地接线证据。

## 未完成项的准确边界

以下不是本阶段遗漏的本地实现，而是需要新授权或外部账号条件的后续轨：真实 ChatGPT Developer Mode 安装/Scan Tools、Platform Secure MCP Tunnel、稳定公共 HTTPS/OAuth、真实用户数据库迁移、上游 Candidate 合并、Apple 签名/公证与生产发布。在取得对应回执前不得把 `BLOCKED_EXTERNAL_ACCOUNT` 或 `C_MIGRATION_REQUIRED` 改写为完成。
