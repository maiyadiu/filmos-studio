# Track 08 Agent 原生多脑计划

权威实施计划位于 `implementation/agent-native-multibrain/`。执行顺序固定为：合同与 Registry → Context/Session → Codex/API/Hosted Adapter → Tool/Policy/Audit → 原生 UI → 迁移/回滚 → 全量验收。

Feature Flags 初始默认关闭；旧 UI、旧 Codex 路由和旧偏好至少保留一个候选周期。

