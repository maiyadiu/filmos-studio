# 多脑纠偏检查点

Current objective：以固定 `9267a5f1…` 完成 Agent 原生多脑继承式纠偏全部 WP/Gate。

Active workspace：`/Users/apple/Downloads/other/短剧/wt-agent-native-multibrain-v11-final`

Branch：`fix/agent-native-multibrain-v11-final`

Locked decisions：

- 影策原生 Agent Panel 是唯一入口。
- Codex Subscription 原生流式；ChatGPT Subscription 真实 Host Handoff。
- ChatGPT Tunnel/Keychain/Grant/MCP/Proposal Foundation 复用和扩展，不复制。
- API/Claude/DeepSeek/兼容渠道完整保留，只有显式 API Profile 才可调用。
- 无订阅到 API 静默 fallback。
- Film Core 是事实源，正式写入必须 Preview/确认/后验/Audit。
- `main` 不改，不创建 RC1。

Completed：WP-00 基线审计/ADR；WP-01 共享合同、Registry 与隔离 Session；WP-02 显式 Workbench Context/Receipt/Grant；WP-03 Codex Subscription app-server 原生适配、审批与取消；WP-04 API/本地适配与无静默 fallback；WP-05 单一原生 Panel 六脑选择、按项目+脑会话隔离、Codex 账户/套餐/额度/登录、ChatGPT Hosted Timeline 与 Desktop 连接入口。

Next：WP-06 扩展现有 ChatGPT Host Foundation 的全局 Tunnel/按项目 Session、真实 MCP Manifest、Grant 生命周期与 Proposal Handoff；随后 WP-07 统一 Tool/Policy/Audit、WP-08 迁移回滚、WP-09 全量验收。

Open external gates：真实 Codex 订阅低风险 Golden；真实 ChatGPT Host Live Gate。任何账号/真实额度步骤必须使用现有授权并保留原始 receipt，不用 Mock 冒充。

Do not do：不跟随漂移 integration/upstream；不并回 main；不创建 Tag；不使用 Cookie/WebView/私有接口；不触发未授权模型 API。
